# UI/UX SPEC — AL FAROUQ AI

> آخر تحديث: 2026-09-22 — الكود في `web/`.

## 1. المبدأ

«الطالب يتعامل مع مدرس AI حقيقي» — واجهة نظيفة تشبه حجرة تعليمية وليست Dashboard إدارية.

## 2. تجربة البداية (Onboarding سريع)

1. **Auth**: شاشة دخول/تسجيل بسيطة (email + password + displayName).
2. **اختيار المسار**: يشبه خريطة: بلد → نظام → صف → مادة → منهج → وحدة → درس — خطوات كبيرة وقابلة للنقر، مع Breadcrumb عربي.
3. **الدرس**: شاشة تفاصيل الدرس (المفاهيم)، زر «ابدأ المذاكرة مع المعلم».

## 3. غرفة الدراسة (القلب)

- **اسم المدرس**: «سلاح الفرعون» — شخصية ثابتة ودودة.
- **واجهة دردشة** عربية RTL: فقاعات الطالب يسار، فقاعات المعلم يمين (بالعربي)، توقيتات خفيفة.
- **أنواع الفقاعات**:
  - نص شرح نصي، سؤال متابعة، **تلميح** (أيقونة 💡 وسياق «تلميح»)، مثال، تمرين.
  - كل رسالة تعليمية يمكن أن تحمل «فهمت ✅ / مش فاهم 🤔» (زران تفاعليان) → يرسلان intent للـTutor.
- **شريط سياقي** أعلى الشاشة: الصف • المادة • الوحدة • الدرس (يطمئن الطالب أنه في مكانه الصحيح).
- **رابط الاقتباس**: عند الحاجة، رسالة توضيح «مصدر: درس كذا، منهج كذا» (يبني الثقة) — упоминание المصدر في الرد.

## 4. نظام التصميم

- CSS خالص (Custom Properties) — بلا مكتبات UI ثقيلة؛ ثيم فاتح دافئ (نجوم / ألوان صحراوية).
- **RTL إلزامي**: `dir="rtl"` في الجذر، خطوط عربية آمنة (system stack + `Segoe UI` بفولباك عربي).
- Motion خفيف، ألوان متباينة (WCAG AA)، تصميم responsive (هاتف/تابلت/شاشة).
- أخطاء وضوحها موثوقة وودودة (لا رسائل تقنية).

## 5. التوسع المستقبلي (بنية الواجهة جاهزة)

| العنصر | الحالة |
|---|---|
| صوت (Voice conversation) | فتحات `components/voice/*` بمواصفات فارغة موثقة |
| صورة/سؤال مصور | فتحة في نموذج الإدخال |
| تمارين تفاعلية | فئات `ExerciseCard` (بنية جاهزة) |
| Progress view | صفحة `Progress` تبين إتقان المفاهيم كنقاط ضوء |

## 6. أداء الواجهة

- Vite dev server مع proxy `/api → localhost:3001`.
- تفاعل خفيف: لا مكتبات حالة ثقيلة؛ fetch + React state.
- أرقام لا تُكشف: لا تظهر مفاتيح أو internals في الـUI أبدًا.