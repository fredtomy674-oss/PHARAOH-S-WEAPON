# DECISIONS — AL FAROUQ AI

> سجل القرارات المعمارية: **ماذا قررنا، لماذا، وماذا لو تغيّر؟** كل قرار يكتب بعد اتخاذه.

## D-001 — اللغة والبنية العامة
**القرار**: TypeScript للخادم والواجهة في npm workspaces (`server/` + `web/`).  
**لماذا**: لغة واحدة تقلل التبديل السياقي؛ Node 24 موجود؛ أفضل توافق مع AI coding agents وإعادة الاستخدام الشامل للنماذج.  
**بدائل**: Python(uv) — ممتاز للـAI لكنه يشطر النظام إلى لغتين.  
**المقارنة**: Maintainability=TS ✅, Agent-friendliness=TS ✅, Cost=متساوٍ, Performance=كافٍ.  
**فضلاً**: لا تغيير دون مشكلة معيارية حقيقية.

## D-002 — إطار السيرفر
**القرار**: Fastify 5 بدل Express.  
**لماذا**: JSON-schema validation مدمجة (Ajv، آمنة واحترافية)، plugin architecture نظيفة، أداء ممتاز، hooks لـauth/rate-limit.  
**بدائل**: Express 5 (شائع جدًا لكن validation يدويًا)، Hono (جديد، أقل نضجًا في plugins).  
**المخاطر**: عدد plugins أصغر قليلًا — مُدار بالتوثيق الرسمي.

## D-003 — قاعدة البيانات والمigrations
**القرار**: Drizzle ORM + drizzle-kit فوق `better-sqlite3` محليًا، مع مسار مهاجرة موثق إلى PostgreSQL.  
**لماذا**: SQLite=صفر إعداد على جهاز بلا Docker ولا Postgres؛ Drizzle: schema كنوع TypeScript نقية (agent-friendly)، migrations قابلة للقراءة، بلا محركات ثنائية (تجنب مشاكل Windows كما في Prisma).  
**بدائل**: Prisma (أثقل، binary engines)، Kysely (query builder فقط، بلا schema DSL كامل).  
**إلى الإنتاج**: تبديل driver في `db/index.ts` + مرآة schema-postgres (TECHNICAL_ARCHITECTURE §6). Migration-first: جديد schema = توليد migration + تطبيقه.

## D-004 — Vector Store
**القرار**: `SqliteVectorStore` ينفذ interface `VectorStore` (بحث تشابه ناقل في JS على ناتج SQL فلتر Metadata).  
**لماذا**: صفر بنية خارجية، دقة بمقياس MVP (≤100k chunk) مناسبة، وفصل كامل يسمح لاحقًا بـ pgvector/Chroma/Qdrant بدون لمس `rag/`.  
**لماذا لا sqlite-vec**: إضافة origin أصلية على Windows قد تعقد التشغيل (متغير؛ mapped env للتفعيل لاحقًا إن رغبنا).

## D-005 — AI Provider abstraction
**القرار**: Interfaces `LLMProvider` و `EmbeddingProvider`؛ تنفيذ `Mock*` (أوفلاين، للاختبارات والتطوير) و `Gemini*` (حقيقي). Routing بالنموذج حسب العملية عبر `AI_ROUTING_*`.  
**لماذا**: تغيير المزود = ملف + env، بلا إعادة بناء. Mock يسمح بـ vertical slice كامل بلا مفاتيح.  
**ملاحظة**: لا يُرسَل محتوى خارجي في الاختبارات؛ الاختبارات الحية opt-in `RUN_LIVE_TESTS=true`.

## D-006 — المصادقة
**القرار**: جلسات DB (جدول `sessions`) مع cookie httpOnly + CSRF token + bcryptjs للهاش.  
**لماذا**: قابلية الإلغاء فورية، تحكم بالصلاحيات خادميًا، مناسبة لمنتج قصّر، بلا اعتماد JWT stateless (صعوبة إلغاء).  
**بدائل**: JWT (رد عليّ بالإلغاء), Lucia (مكتبة رائعة لكن تعتمد على adapter آخر — نستغني عنها لصالح 100% تحكم).  
**ملاحظة**: bcryptjs نقية (بلا أصلية بناية على Windows)؛ يمكن ترقية المعاملات production في الـenv لاحقًا.

## D-007 — الـRAG: Metadata هو الحاجز
**القرار**: كل chunk يحمل Metadata كاملة (country→concept, source, version) و Retrieval يرفض أي استدعاء بلا scopeFilter كامل.  
**لماذا**: يمنع «المنهج الخاطئ» على مستوى التصميم، لا بالاعتماد على النموذج.

## D-008 — الواجهة
**القرار**: React 19 + Vite 6 + TypeScript، CSS خالص (RTL-first) عربي.  
**لماذا**: قلة الاعتماديات، تحكم كامل بثيم RTL، سرعة Build، وواجهات أنواع الصوت/الصور كفتحات موثقة.  
**بدائل**: Next.js (monolith يُضْعِف فصل المكونات)، MUI (ثقيل عن الحاجة).

## D-009 — التجارب/التهيئة
**القرار**: Vitest + inject + Mock providers كل شيء قابل للتشغيل أوفلاين. Rate limit يومي للطالب على رسائل AI.  
**لماذا**: التكلفة محدودة (`DAILY_MESSAGE_LIMIT`)، والاختبارات مستقرة (لا شبكة).

## D-010 — إدارة التكلفة
**القرار**: كل استدعاء LLM يمر عبر `UsageTracker` (سجل `ai_usage_logs`) + caching في `AiCache` للاستجابات الشائعة + router يختار النماذج الرخيصة لمهام معينة (classifier أصغر).  
**لماذا**: cost-awareness صبريًا؛ تقدير التكلفة يُحسب من عمود النموذج في حالة توفره.

## D-011 — قبول ثغرات dev-only المتبقية (4 moderate)
**القرار**: قبول 4 ثغرات moderate متبقية في `npm audit` — كلها dev-only عبر سلسلة `drizzle-kit → @esbuild-kit/esm-loader → @esbuild-kit/core-utils → esbuild@0.18.20`.  
**لماذا**: لا تدخل في حزمة الإنتاج runtime؛ لا شيء هنا يستخدم `esbuild serve` (الوضع المتأثر بالثغرة) — vite وtsx يستخدمان transform فقط، وكل مثيلات esbuild الأخرى موسّعة (0.25.12/0.28.2).  
**جولة 2026-09-22 (تحقّق إضافي)**: حُوّل لإغلاقها عبر `overrides` (رفع esbuild المتداخل إلى إصدار مصحح) — أُثبت ميكانيكيًا في عزلة أنه يعمل، لكن على شجرة المشروع الحقيقية (مسارات متعددة: vite + tsx + drizzle-kit) يتجاهل npm الـoverride في فحص `npm ls`/`npm ci` ويُخرج `ELSPROBLEMS invalid` (حزمة core-utils متروكة `deprecated: Merged into tsx`، ونطاقها `~0.18.20` ثابت). لذا: **إما audit نظيف أو `npm ls`/`ci` سليم — لا كلاهما** عبر overrides. رُفض تعقيد patch-package لسلسلة dev-only → الإبقاء على D-011.  
**فضلاً لاحقًا**: عندما يُسقط drizzle-kit `@esbuild-kit/esm-loader` (أو يُستبدل core-utils) → `npm audit fix` وإغلاق السجل.

## D-012 — سلوك Fastify مع JSON فارغ + مواصفات جسم الـAPI
**القرار**: عميل الويب يرسل `Content-Type: application/json` فقط عند وجود جسم فعلي.  
**لماذا**: Fastify يرفض `POST` يحمل content-type=json مع جسم فارغ (400 `FST_ERR_CTP_EMPTY_JSON_BODY`). أثر ذلك: `logout`/`endSession` كانت ستفشل في الويب قبل الإصلاح.  
**ملاحظة للاختبارات الخارجية**: المحاكاة عبر curl بلا content-type بلا جسم تعمل (`{"ok":true}`)؛ النقطة النهائية idempotent (إنهاء جلسة منتهية لا يعيد خطأ).

## D-013 — بنية Browser E2E (Playwright) + rate-limit قابل للضبط
**القرار**:
1. اختبارات Playwright على مستوى جذر workspace (`e2e/` + `playwright.config.ts`)، تشغّلها أمر `npm run e2e` بخادمين مُدارين تلقائيًا:
   - خادم اختبار على منفذ **3107** ضد قاعدة SQLite مؤقتة (تُحذف وتُزرع كل تشغيل عبر `e2e/reset-db.mjs` + seed) — **لا تمسّ قاعدة التطوير** `server/data/alfarouq.sqlite`.
   - Vite حقيقي على **5173** (ميناء التطوير القياسي) ببروكسي `VITE_API_PROXY_TARGET` (افتراضي خارج الاختبارات: 3001 — بدون تغيير سلوك التطوير).
2. `RATE_LIMIT_MAX` أصبح متغير env (الافتراضي 120/دقيقة)؛ `allowList` يستثني `/api/health`؛ معالج الأخطاء يمرر حمولة `error` الصادرة من rate-limit كما هي (كانت تتحول إلى 500 عام).  
**لماذا**: اختبار E2E حقيقي يستحق عزلًا كاملًا للبيانات؛ مجموعة آلية + health-poll تتجاوز حد 120/دقيقة بسهولة → 429 كاذبة. ورسالة «طلبات كثيرة» الواضحة كانت تضيع من المستخدم.  
**بدائل**: تشغيل E2E فوق قاعدة التطوير (يرفضه المستخدم: لا تفسد بيانات التطوير)؛ معدل ثابت مرتفع (يُضعف الحماية في الجميع).  
**ملاحظات**:
- `workers: 1` + `fullyParallel: false` إجباريان (قاعدة مشتركة + حساب تجريبي واحد).
- المتصفح: Chromium (افتراضي) — القابل للتوسعة عبر `projects`.
- حقل الدردشة `maxLength=4000` يمنع تجاوز الطول من المستخدم حقًا؛ لذلك اختبار «خطأ API واضح» استخدم سيناريو CSRF منتهي (E) بدل رسالة طويلة (كانت ستمر عبر المتصفح قبل الوصول للخادم).

## D-014 — Vision Upload (سؤال مصور): base64-JSON في MVP + تخزين BLOB + نقاط أمان
**القرار** (PHASE 10):
1. الرفع عبر `POST /messages` في JSON الحالي بجسم `image: { dataUrl, fileName }` (base64) بدل multipart — يبقي العقود والحدود (schema zod + Fastify validation) والـCSRF في مكانها.
2. التخزين BLOB في جدول `message_attachments` (message_id فريد، session_id للعزل/الحذف، sha256 + size + mime) — بلا نظام ملفات خارجي، يخدم قاعدة الاختبارات المؤقتة بلا أثر.
3. الخدمة عبر `GET /sessions/:id/attachments/:attId` بفحص ملكية (`getOwned`) + رؤوس `content-type` (قائمة بيضاء)، `x-content-type-options: nosniff`, `content-security-policy: default-src 'none'; sandbox`, `cache-control: private`.
4. قائمة بيضاء صارمة للصيغ (PNG/JPEG/WebP) + حد `MAX_IMAGE_KB` (افتراضي 5000؛ الاختبارات 1)؛ رسالة بلا نص مقبولة إذا وُجدت صورة.
5. AI: `LLMRequest.images` (مستقل عن cache/classifier) → Gemini `inlineData`؛ mock يصدر عبارة «قرأت الصورة المرفقة» حتمية لأتمتة الاختبارات.
6. Grounding: عندما يكون النص فارغًا مع صورة، الاسترجاع يستخدم استعلامًا محايدًا («سؤال مصور في هذا الدرس») فلا ينخفض `contextChunkCount` إلى صفر.
**لماذا**: MVP بسيط وقابل للاختبار عبر المسار الحقيقي كاملًا (Browser→proxy→Fastify→SQLite→AI→GET بالمستخدم)؛ لا نفترض مزود تخزين خارجي بعد.
**بدائل**: multipart upload (يضيف `@fastify/multipart` وقواعد حجم/تعقيد للعقود)؛ تخزين ملفات على القرص (تعقيد إدارة مسار في الاختبارات المؤقتة) — نُرجئان للإنتاج.
**إنتاج لاحقًا**: multipart + تخزين كائنات + فحص MAGIC bytes أعمق (مستوى الصورة «تدقيق سطحي»).

## D-015 — Voice conversation (سؤال بصوت): Web Speech API في المتصفح (لا مزوّد خادمي)
**القرار** (PHASE 11):
1. طبقة الصوت **كاملة في المتصفح** عبر Web Speech APIs: STT بـ `SpeechRecognition`/`webkitSpeechRecognition` (عربي `ar-EG`) وTTS بـ `speechSynthesis` — بلا مفاتيح، بلا تغيير في الخادم (نفس `POST /messages`)، وبلا جدول/env جديد.
2. مسار المستخدم يطابق معيار القبول حرفيًا: «طرح سؤال بصوته» (زر 🎙️) → «مراجعة النص الناتج» (النص يُنسخ للحقل للمراجعة/التعديل) → «إرساله» (الطلب العادي) → «سماع رد المدرس وإيقافه» (نطق تلقائي للرد بعد سؤال صوتي + زر 🔊 لكل فقاعة + ⏹ إيقاف عبر `speechSynthesis.cancel`).
3. تغذية راجعة أمنية للاستخدام: رسالة واضحة عندما يكون STT «غير مدعوم في هذا المتصفح» (Firefox مثلًا) ورسائل خطأ عربية لكل `error` (رفض الميكروفون/لا صوت/شبكة) — يبقى الإرسال النصي وVision يعملان دائمًا.
4. أتمتة E2E عبر stubs متحكمة (`installVoiceStubs`) تحقن `SpeechRecognition` (نطق واحد ثم end) و`speechSynthesis` (تسجيل speak/cancel بلا صوت) — لا يمكن أتمتة ميكروفون/سماعة حقيقيين بشكل حتمي؛ المسار الحقيقي (Vite→Fastify→SQLite→RAG→AI) يبقى كما هو.
**لماذا**: Web Speech API مجاني ومدمج في Chromium/Edge، يعمل على localhost/HTTPS (secure context)، ويلبي معيار القبول دون اشتراك خادمي (تكلفة صفرية لخدمات STT/TTS سحابية، ولا مفاتيح في التطوير). المسار الخادمي (مثلاً Gemini audio) كان سيزيد تكلفة/تعقيدًا بلا حاجة وظيفية حاليًا.
**بدائل**: STT/TTS خادمية عبر مزوّدي AI (خارج Gemini inlineData للنصوص) — تُرجئ كـ«ترقية» إذ تبقى واجهات AI جاهزة؛ ترميز صوت العميل وإرساله (واجهة بيانات أكبر وخوادم وسائط).
**إنتاج لاحقًا**: مزوّد STT/TTS خادمي عبر واجهات AI (قرار مفتوح: أي خدمة)، تخزين وتشغيل أصوات مخصّصة، واختيار صوت عربي تلقائي أفضل ثم ترتيب أولوياته.
**ملاحظات مطبقة**: محرك Chromium يرمي `TypeError` عند تعيين كائن لا يطابق عقد `SpeechSynthesisVoice` على `utterance.voice` (أصوات الـstub) → تعيين `lang` أولًا + تعيين الصوت داخل try/catch. React StrictMode في dev يعيد تركيب الغرفة فيُشغّل cleanup «مغادرة الغرفة» (الذي يوقف الصوت) مبكرًا → اختبارات الإيقاف تقيس الإلغاء **نسبيًا** لا قطعيًا.

## D-016 — Student Documents in Chat (PHASE 12, Path A): مرفقات ملفات → استخراج نص آمن
**القرار**:
1. **المسار**: ملفات الطالب تُرفع **داخل الدردشة** (Path A) عبر نفس `POST /messages` بصيغة base64-JSON مثل Vision: `document: { dataUrl, fileName }`؛ **صورة XOR مستند** ← 400 `MULTIPLE_ATTACHMENTS`. مسار B (إدخال PDF/DOCX في قاعدة المعرفة) **يُبقى منفصلًا وغير ممسوس**.
2. **التخزين**: إعادة استخدام `message_attachments` + عمود `extracted_text` nullable جديد (migration `0002_material_virginia_dare.sql`)؛ الحدود عبر env: `MAX_FILE_KB` (10000) و`MAX_DOCUMENT_CHARS` (20000)، و`bodyLimit` لـFastify → 32MB.
3. **استخراج النص خواص JS نقية بلا نظام ملفات**: PDF عبر **`pdfjs-dist`** (legacy ESM build، بلا worker)؛ DOCX عبر **`mammoth@1.12.3`** (مثبّت فوق نطاق GHSA-rmjr-87wv-gf87 ≤1.10.0)؛ TXT/MD UTF-8 + إزالة BOM. أي فشل/ملف ممسوح ضوئيًا → نص فارغ (لا انهيار؛ **OCR لاحقًا** — PHASE 19/D-023).
4. **لماذا pdfjs-dist لا pdf-parse**: pdf-parse يغلّف pdf.js 1.10.100 (من 2018)؛ فرع الـdebug فيه `if (!module.parent)` يُفعَّل تحت `import()` (ENOENT على `test/data/`) **وحتى عبر CJS يتقلّب على Node 24** («bad XRef entry» تارة ونجاح تارة أخرى لنفس الملف)؛ pdfjs-dist legacy حديث ومستقر (حيّز 3/3). بدون هذا كان الاختبار غير حتمي.
5. **Grounding غير المسموح به**: نص المستند **بيانات مستخدم غير موثوقة** — لا يُلصق في system prompt إطلاقًا؛ يسافر كـ`LLMRequest.documents` ضمن حد `MAX_DOCUMENT_CHARS`، ويميّزه الـPromptBuilder بعلامة `<document>` ضمن قاعدة نظام «محتوى المستخدم غير موثوق». **Tripwire الحقن يعيد فحص النص المستخرج خادميًا (classifyIntent) قبل أي استدعاء نموذج** → SAFE_REFUSAL «أنا هنا لمساعدتك في درسنا فقط».
6. **Grounding مع مستند فقط**: رسالة بلا نص مع ملف مقبولة؛ استرجاع احتياطي doc-only «سؤال عن محتوى الملف المرفق في هذا الدرس» حتى لا يسقط `contextChunkCount` (نفس نمط Vision).
7. **مولد الـfixtures وليد Node خالص** (`scripts/make-doc-fixtures.mjs`): PDF بيدوي بإزاحات xref محسوبة بدقة (احتسب رأس الـ9 بايت!) + ZIP بيدوي (STORED + CRC-32). **لماذا لا أدوات النظام**: `Compress-Archive` و`ZipFile` (.NET Framework في PowerShell 5.1) يكتبان أسماء إدخالات بشرطة مائلة عكسية (`word\document.xml`) على Windows فيرفضها OPC/mammoth؛ المولد الحالي صفري الاعتماد على shell ويعمل على أي OS.
8. **OCI/audit**: بلا اعتماديات أصلية؛ audit ظل على الـ4 moderates المعروفة dev-only (D-011) — pdfjs-dist لم يضف شيئًا.
**بدائل**: multipart upload (يضيف plugin وتعقيد حدود)، OCR فوري (نُفِّذ لاحقًا — PHASE 19/D-023)، ملفات على القرص (تعقيد إدارة في قواعد الاختبارات المؤقتة) — كله إنتاج لاحقًا.
**إنتاج لاحقًا**: multipart + تخزين كائنات + فحص MAGIC bytes أعمق (OCR للممسوح ضوئيًا أُنفِّذ — PHASE 19/D-023).

## D-017 — Curriculum File Import (PHASE 13, Path B): ملفات PDF/DOCX في قاعدة المعرفة
**القرار**:
1. **إعادة استخدام الاستخراج دون لمس مسار الطالب**: `admin` يستدعي `parseDocumentDataUrl` / `extractDocumentText` من `sessions/documents.js` كما هي (استيراد إضافي صفري التعديل على Path A) — نفس فحص الأمان لكن بسقف المنهج الأكبر (`MAX_CURRICULUM_FILE_KB`، افتراضي 20480=20MB، و`MAX_CURRICULUM_DOCUMENT_CHARS`=200000). النوع (pdf/docx/text) يُستنتج من mime في dataUrl؛ كل ما عداه يعامل كـ text.
2. **الهوية والبايتات**: `document_versions.data` (BLOB) تحفظ البايتات الخام للملف، و`sha256` = تجزئة البايتات الخام — هي **هوية الملف** لإزالة التكرار (نفس البايتات + نفس المنهج → `409 DOCUMENT_ALREADY_INGESTED`)؛ مستقلة عن تجزئة المحتوى السكانية داخل chunks.
3. **إزالة التكرار داخل chunks أصبحت مرتبطة بالدرس**: استُبدل الفهرس الفريد العام `chunks_content_hash_unique` بفهرس مركب `chunks_content_hash_lesson_unique (content_hash, lesson_id)` (migration `0003_many_namor.sql`) — نصٌّ واحد يُدرج في درسين مختلفين فيصبح مسموحًا (قرار تعمّد، لعدم كسر دروس مشاركة محتوى).
4. **مصير نص الملف**: يمر بنفس أنابيب chunking/embedding/scoping كالنص، ويُسترجَع في **`<context>` فقط** — جمل تجاوز بداخله لا تُنفَّذ (S6 عبر الملف؛ دون رفض عند الإدخال: `context` اسمه محتوى منهج بموجب قاعدة النظام). استخراج صفري للممسوح ضوئيًا → **OCR (PHASE 19/D-023)** يحاول الإنقاذ أولًا، وإلا `400 EMPTY_DOCUMENT` مشروح بوضوح (لا انهيار).
5. **الاختبارات**: `knowledgeFile.test.ts` (وحدة: pixels/بايتات/دوبليكات/عزل درس) + `adminFile.test.ts` (API: 201/403/خطوط الحدود/استرجاع فعلي/عزل عبر lessons/S6). **بلا E2E هذه المرحلة** وفق الموافقة. سقف الاختبارات `MAX_CURRICULUM_FILE_KB=4` لاختبار رفض الحجم.
**لماذا**: المسار المركّز لوارد المناهج (Tensor أدمن يرفع الملفات الكاملة) يريد PDF/DOCX بلا نظام ملفات خارجي ولا اعتماديات أصلية؛ إعادة استخدام الأدوات الموجودة يبقي Path A ثابت السلوك ويكرّر أمانًا مُختبَرًا.
**بدائل**: multipart upload (يضيف plugin وتعقيدًا؛ مؤجل للإنتاج)؛ OCR فوري (نُفِّذ لاحقًا — PHASE 19/D-023)؛ ملفات على القرص (تعقيد في قواعد الاختبارات المؤقتة).
**ملاحظة (إصلاح مواكب)**: أصلح مزوّد `mock` لاستخراج كتلة `<context>` الحقيقية (آخر وسم تواجد) بدل أول تواجد داخل نثر قواعد النظام — صدى الرد في التطوير أصبح مطابقًا للمحتوى الفعلي المسترجع (بلا تأثير على مسار الطالب في الزمن الآني).
**إنتاج لاحقًا**: multipart + تخزين كائنات + فحص MAGIC bytes أعمق (OCR للممسوح ضوئيًا أُنفِّذ — PHASE 19/D-023).

## D-018 — Admin Dashboard (PHASE 14): واجهة استيراد ملفات المنهج
**القرار**:
1. **شاشة واجهة خالصة في `web/` بلا توجيه URL**: إضافة `Screen = "admin"` في `App` + زر «لوحة الإدارة» في `Home` يظهر **فقط** حين `user.role === "admin"` (اختبار A3 يثبت أنه لا يظهر للطالب إطلاقًا) — لا مسار عمومي جديد ولا auth إضافي؛ الحصانة الأمنية الحقيقية تبقى خادميًا (`requireAdmin` على كل endpoint).
2. **نقل الـscope عبر نفس نمط picker**: تعاد نفس سلسلة القوائم (دولة→نظام→صف→مادة→منهج→فصل→وحدة→درس) داخل شاشة الإدارة، والدرس المختار هو `nطاق` الإدخال — لأن `ingest-file` يفرض `lessonId` (وثيقة = درس واحد). لم يُستخرج مكوّن مشترك لتجنب أيّ خطر انحدار على onboarding الحالي (تكرار مقبول في هذا النطاق الصغير).
3. **رفع الملف client-side → dataUrl**: `FileReader.readAsDataURL` + مرآة سقف المنهج (20MB) على العميل؛ قدوم البيانات كـ base64-JSON يطابق قرار D-014 (لا multipart في MVP) والـschema الحالي (`dataUrl ≤ 28_000_000`).
4. **إثراء قائمة المستندات**: `GET /api/admin/documents` أصبح يجرّ агрегиت الـchunks (`LEFT JOIN` + `count`) ليعرض `lessonTitle` و`chunkCount` — اللوحة بلا هذا الرابط لا تكفي لإدارة القاعدة؛ «ready» فقط كما كان.
5. **عزل E2E**: اختبارات الإدارة تستهدف **الدرس الثاني** (الضرب والقسمة) في الـseed كي لا تختلط مقاطع ملفات Path B بنطاق الدرس الأول المشترك بين بقية المجموعة (لا انحدار على افتراضات RAG الموجودة).
**لماذا**: يكمل PHASE 13 ليكون الإدخال قابلاً للاستخدام الفعلي من دون أدوات خارجية، ويُغلق حلقة «نقطة نهاية موجودة بلا واجهة» بأقل تغيير وباحتفاظ كامل بالحصانة الخادمية.
**بدائل**: توجيه URL منفصل/راوتر (يعقّد SPA الصغير بلا مكسب)؛ مكوّن picker مشترك (يلمس Onboarding بلا داعٍ)؛ إخفاء الواجهة للمستخدمين غير admin خلف 403 خادمي (ما زال قائمًا — الواجهة مجرد تجربة ألطف).

## D-019 — حماية رفع الملفات (PHASE 15): فحص MAGIC bytes / توقيع الملفات
**القرار**:
1. **الوكيل الوحيد «الصادق» هو البايتات**: MIME في data-URL **مُعلَن ذاتيًا** (يبنيه المتصفح من الامتداد)، لذا يُشمّ النوع الفعلي من البايتات الخام وتُقارَن بالتوقيع المتوقع — تطابق فقط يمضي؛ أي انتحال (نص→PDF، ZIP عام→DOCX، PDF→نص) → `400 FILE_TYPE_MISMATCH` **قبل أي استخراج أو تخزين** (لا تأكّل النص عبر محلّل خاطئ ولا وسم `kind` كاذب في قاعدة المعرفة).
2. **التوقيعات المعتمدة**: PDF = رأس `%PDF-` خلال أول 1024 بايتًا (مواصفة PDF تسمح بـ junk-prefix)؛ DOCX = رأس ZIP `PK\x03\x04` عند الإزاحة 0 **و** وجود `[Content_Types].xml` في أول 64KB (يميّز OOXML عن أي أرشيف ZIP عام — ZIP بلا الملف المركزي يُرفض كـ `docx` مُزيّف)؛ كل ما عداهما = `text` (نص عادي بلا توقيع، BOM مقبول).
3. **نقطة شائعة للمسارين**: الفحص داخل `parseDocumentDataUrl` نفسها فيغطي A (دردشة) وB (مناهج) دفعة واحدة بلا تغيير في توقيعات الاستدعاء أو معالجات الطرق.
4. **السلوك الناتج**: مستند «ممسوح ضوئيًا» حقيقي (رأس PDF سليم بلا نص) لا يزال يمر (التحقق غير متعلق بالمحتوى النصي) → بقي خط سير `EMPTY_DOCUMENT`/`textChars=0` حتى **PHASE 19/D-023**: صار OCR يقرأه عبر مزود AI (المساران A وB)؛ اختبارات «الممسوح» السابقة استخدمت نصًا ينتحل صفة PDF — صُححت إلى رأس PDF حقيقي ثم إلى fixture صالح بلا طبقة نص.
**لماذا**: الامتداد/نوع MIME هما أول واجهة هجوم على رافعات الملفات؛ تحقق خادمي غير مكلف (مسح رؤوس في الذاكرة بلا محلل) يغلق انتحال الهوية قبل أي معالجة لاحقة ويشدّد كلا مساري الرفع بالتزامن.
**بدائل**: فحص `file`/`magic-sharp` (اعتماديات أصلية — خارج قيد المشروع)؛ تحليل zip كامل لإثبات OPC (مكلف بلا مكسب للتمييز)؛ ثقة في MIME المعلن من المتصفح (هو ما نغلقه).
**إنتاج لاحقًا**: امتدادات (ماكرو غير ضار؟/فحص متعمق داخل zip) وmultipart.

## D-020 — زرع منهج سعودي / multi-country (PHASE 16)
**القرار**:
1. **منظومة البذر أصبحت وصفية-عامة**: `seedCountry(db, knowledge, spec)` يستقبل بصمة `CountrySeedSpec` (دولة/نظام/صف/منهج/فصل/وحدة/دروس بمفاهيمها ومستندات RAG) — مصر تُعرَّف ببصمتها الأصلية حرفيًا، والسعودية ببصمة ثانية، وأي بلد قادم = بصمة إضافية بلا لمس الشيفرة.
2. **الكتالوج العالمي مشترك لا متكرر**: جدول `subjects` بلا countryId (مادة «الرياضيات» واحدة عالميًا) — تزرع كل دولة عبر إعادة استخدام صفّ `math` الموجود وفق قيد `subjects_code_unique`؛ وثّقه اختبار ببذر سعودي فوق مصري في نفس القاعدة.
3. **العزل هيكلي لا حكلي**: مرشحات `countryId` القائمة في خدمة المناهج وscope الـ`lessonId` في RAG هما الأصل — PHASE 16 لم تغيّر خدمةً ولا جدولًا، بل أثبتت بالاختبارات أن الجلسة السعودية لا ترى سوى محتوى سعودي («الرياض» حاضر و«مقارنة الكسور» غائب) والمصرية عكس ذلك.
4. **حتمية E2E بصرف النظر عن الترتيب**: إضافة السعودية جعلتها تسبق مصر أبجديًا فكان «أول خيار» سعوديًا يكسر افتراضات الاختبارات — أُضيف `selectOptionByLabel` (اختيار بالنص لا بالترتيب) بدل تعديل ترتيب العرض في المنتج أو لمس `orderBy`.
**لماذا**: نموذج «سلاح الفرعون» إقليمي بطبيعته (مصر ثم السعودية ثم غيرها)؛ البصمة الوصفية + إعادة استخدام الكتالوج العالمي تبقيان التوسع بلا قفزة معمارية أصلية.
**بدائل**: فصل `subjects` per-country (توسّع جدولي بلا مكسب — المادة مفهوم عالمي)؛ ترتيب الدول لتظل مصر أولًا (يخفي مشكلة الترتيب لا يحلها)؛ نسخ منطق البذر لكل بلد (تكرار برمجي).
**إنتاج لاحقًا**: بقية بلدان الخليج؛ مناهج علوم ولغة عربية لكل بلد؛ فصول دراسية متعددة عند الحاجة.

## D-021 — Analytics المسؤول: تجميعات صافية وقت الطلب (PHASE 17)
**القرار**:
1. تنكشف الإحصاءات عبر **نهاية واحدة `GET /api/admin/stats`** تُركّب عدّادات `count()` من الجداول القائمة لحظيًا (مستخدمو/طلاب، جلسات، رسائل، مستندات، مقاطع، مناهج، دروس) — **بلا جداول تخزين جديدة**: تجميع صافي في اللوحة الحالية (PHASE 14).
2. الحصانة تبقى على **`requireAdmin`** في المسار نفسه (نفس نمط `/admin/documents`)؛ الطالب يُرفض 403 `FORBIDDEN` قبل أي حساب.
3. الواجهة تعرض بطاقات صغيرة («إحصاءات سريعة») في أعلى اللوحة بفواصل `admin-stats-*`، وجلبها **متسامح** (`catch → undefined`) — فشل الإحصاءات لا يمسّ رفع الملفات أبدًا (تحقَّق منه اختبار E2E A1 حتى لا يُكسر التدفق).
4. البقاء بسيطًا قابلًا للتوسع: بنية `stats` مسطحة بأسماء عربية حرة للمعنى؛ أي مؤشر مركّب (معدلات/اتجاهات زمنية) لاحقًا يُضاف حقولًا جديدة لا أعمدتها المخصصة.
**لماذا**: الإحصاءات التشغيلية طلب إداري فوري، والتجميع اللحظي على SQLite بحجم MVP أرخص من صيانة جدول عدّادات يجب مزامنته مع كل كتابة (مصدرًا محتملًا لجفاف/زحف).
**بدائل**: جداول عدّادات مُحدثة عند كل كتابة (دقة أعلى على نطاق ضخم، لكن طبقة مزامنة وتكلفة صيانة بلا حاجة حاليًا)؛ فهرس عدّادات في الذاكرة (تعقيد بلا مكسب). 
**إنتاج لاحقًا**: مؤشرات زمنية (جلسات/يوم)، ترتيب الدروس الأكثر نشاطًا، ولوحة رسوم بيانية عند الحاجة.

## D-022 — Parent Dashboard (PHASE 18): ربط الأبناء برمز مشاركة + قراءة فقط للمجاميع
**القرار**:
1. يربط ولي الأمر طفله بـ**رمز مشاركة قصير** من الطالب ذاته (`students.parentLinkCode` — نص فريد بطول 8 من أبجدية بلا محارف ملتبسة `A-HJ-NP-Z2-9`، يولَّد عند التسجيل ويُعرض في صفحة الطالب) — **لا بريد إلكتروني**: البريد لا يثبت صلة قرابة ويجعله هدف تخمين/تسريب للعزل.
2. تظهر البيانات عبر نهايات `requireAuth` + فحص دور على **وحدة parent مستقلة**: `POST /parent/link {code}` (`400 INVALID_LINK_CODE`، `409 ALREADY_LINKED`)، `GET /parent/children`، `GET /parent/children/:studentId` (هوية + `progressDetail` + ملخصات جلسات)، `DELETE /parent/children/:studentId`. الكود يعمل بـ case-insensitive (يُطبَّع `toUpperCase`).
3. **العزل بنيوي وليس دفاعيًا**: كل قراءة/حذف تعيد التحقق من وجود صف في `students_parents` للوالد نفسه؛ طفل أجنبي → `404` بلا مؤشر وجود (لا تسريب لوجود الحسابات). الطالب لا يصل لواجهات أولياء الأمور (403) والعكس (403 أو قوائم فارغة في `GET /sessions`).
4. **حد MVP**: لا يُكشف محتوى رسائل خام إطلاقًا — عدّادات فقط (`userMessages`/`tutorMessages`/`totalMessages` لكل جلسة) مع العنوان والوقت والحالة؛ تفاصيل المحادثة (أو أي مستوى أعمق) لاحقًا كمرحلة مستقلة.
**لماذا**: أولياء الأمور هم الركيزة الثالثة للمنتج (بعد الطالب والمعلم/الإدارة)، والنموذج (`parents` + `studentsParents`) مهيّأ منذ PHASE 2 — البناء يُكمِل البنية دون تجاوز عزل الأدوار القائم، ويبقى المحتوى الخام خاصًا بالطالب في MVP.
**بدائل**: الربط بالبريد (لا يثبت قرابة، خطر وجودية أوراكل)؛ `requireParent` عام عبر preHandler مشترك (أُبقيت الفحوص في كل مسار لتقليل السطح السطحي). 
**إنتاج لاحقًا**: دعوة ولي أمر بالبريد/رقم البلد للتحقق، تفاصيل محادثة بموافقة الطالب، إشعارات تقدم، وأدوار قرابة متعددة في `studentsParents.relationship` (مهيّأة بالعداد الافتراضي `parent`).

## D-023 — OCR للمستندات الممسوحة ضوئيًا (PHASE 19, Path A + Path B): عبر مزود AI، بلا rasterization
**القرار**:
1. **القراءة عبر تجريد AI الموجود**: `OcrProvider` جديد (واجهة `ocr(request)` + `AiService.ocr()` كنقطة دخول واحدة تسجّل الاستخدام `operation="ocr"`) — **لا محرك OCR محلي ثقيل** (tesseract.js وأمثاله يتطلب WASM/شبكة لبيانات لغوية ويبطئ الاختبارات). `MockOcrProvider` يعيد نصًا عربيًا **حتميًا** (يحتوي `OCR_TEXT_MARKER` ومشتق من تجزئة البايتات فيتمايز ملف عن آخر — بلا تكرار محتوى في الكتل) ويبقي المجموعة أوفلاين؛ `GeminiOcrProvider` يرسل الملف **inline** (`inlineData` — Gemini يقبل `application/pdf` و`image/*` مباشرة) كما تفعل Vision بالصور تمامًا.
2. **بلا rasterization في هذه المرحلة**: تحويل صفحات PDF إلى صور يتطلب canvas/اعتماديات أصلية — خارج قيد المشروع؛ قراءة `application/pdf` inline من المزوّد تغني عنه (قيد موثق: حدود نموذج/حجم تُدار عبر `MAX_OCR_CHARS` + `MAX_FILE_KB`). حدّ «مؤجل» المكونات الثقيلة يزول بهذا الشكل.
3. **متى يُفعَّل** (المساران معًا): فقط عند **فشل الاستخراج النصي** (نص فارغ أو < 40 حرفًا) **و** نوع MIME مؤهّل (PDF/DOCX — `OCR_ELIGIBLE_MIMES`). TXT/MD لا تُقرأ OCR إطلاقًا (ملف قصير صادق يبقى `EMPTY_DOCUMENT`). الملفات ذات الطبقة النصية لا تلمس OCR (اختبار يثبت صفر calls).
4. **النص المعترف به = محتوى غير موثوق، يُعامَل كنص مستخرج تمامًا**: المسار A يحفظه في `extractedText` (عمود `messageAttachments.ocrApplied` الجديد — migration `0005_zippy_logan.sql` → شارة واجهة «نص ممسوح ضوئيًا») ويُمرّره للمُدرّس كمدخل مستند **ويُعاد فحصه بتريبواير الحقن** قبل أي استدعاء؛ المسار B يمرره في أنابيب chunking/embedding ليُسترجَع كمحتوى `<context>` فقط (لا تعليمات). فشل المزوّد → هبوط آمن إلى دلالات «بلا نص» (لا انهيار للجلسة/الاستيراد).
5. **Fixture**: `e2e/fixtures/scanned.pdf` = PDF **صالح تمامًا** (صفحة واحدة، xref محسوبة) **بلا طبقة نص** (content stream فارغ) — يطابق شكل المسح الضوئي للاستخراج؛ يُنتَج من `scripts/make-doc-fixtures.mjs` (`blankPdf`) وتثبّت الاختبارات أن pdfjs يستخرج منه `""`.
**لماذا**: رفع «OCR مؤجل» منذ PHASE 12/13 لأهم الواردات الواقعية (أوراق ممسوحة/صور صفحات) بأقل تعقيد وأقصى اتساق مع قرارات المشروع (واجهات AI + mock أوفلاين + لا اعتماديات أصلية + لا خوادم وسائط).
**بدائل**: tesseract.js محلي (WASM + بيانات لغات عبر الشبكة + بطء — يكسر الأوفلاين الحتمي)؛ تنسيق صفحات PDF محليًا إلى PNG ثم Vision (rasterization أصلية/معقدة)؛ رفض الممسوح نهائيًا (حالة ما قبل PHASE 19).
**إنتاج لاحقًا**: rasterization خادمية متوافقة مع الاختبارات عند ضخامة الملفات، مزوّد OCR مخصّص آخر عبر `AI_OCR_PROVIDER` (و`GEMINI_OCR_MODEL`/`MAX_OCR_CHARS` جاهزان الآن)، وشارة بلاي خاصة بالـOCR في الإحصاءات.

## D-024 — Billing/Subscriptions بلا بوابة دفع + Achievements (PHASE 20): تفعيل الجدولين الجاهزين
**القرار**:
1. **الجداول كانت جاهزة منذ PHASE 2 — هذه المرحلة تُفعّلها فقط**: `subscriptions` (طالب/خطة/حالة/بدء/انتهاء) و`achievement_definitions` + `achievements` (فهرس فريد `student+definition`) — مثل نمط PHASE 18 مع `parents`. الإضافة الوحيدة للمخطط: فهرس فريد `achievement_definitions_code_unique` (migration `0006_sharp_toad.sql`) لضمان بذر حتمي بالكود.
2. **الفوترة MVP = خطط + حدود + منح يدوي، بلا بوابة دفع**: صفّ الاشتراك يُنشأ **كسولًا** لكل طالب (`free`/`trialing`) عند أول قراءة؛ الحد اليومي للمعلّم مشتق من الخطة الفعلية — `DAILY_MESSAGE_LIMIT` للمجاني، `PREMIUM_DAILY_MESSAGE_LIMIT` (افتراضي 0 = بلا حدود) للمميز. الخطة المميزة «سارية» فقط عند `status ∈ trialing|active` **و** غير منتهية (`expiresAt` ماضٍ، أو `status ∈ past_due|cancelled` → هبوط تلقائي لحدود المجاني). الإدارة تمنح/تلغي عبر `PUT /api/admin/subscriptions/students/:id` (سجل تدقيق `subscription.update`)؛ يُقرؤها الطالب عبر `GET /api/me/subscription`. **بلا Stripe/خدمات خارجية** — بوابة الدفع الحقيقية مرحلة مستقلة لاحقًا.
3. **التنفيذ**: `SubscriptionService` يغذّي حد `TutorEngine` عبر حقل `dailyLimit` جديد في `TutorHandleInput` (الانعكاس الصفري على منطق الـ429 القائم — المصدر فقط تغيّر)؛ رسالة «الحد اليومي» تتحدث بالرقم الفعلي للخطة.
4. **الإنجازات تُنفَّذ خادميًا بأحداث دورة حياة، بمنحٍ موحّد مضاد للتكرار**: تعريفات الثبات بذر حتمي بالكود (أول خطوة=جلسة منتهية≥1، مستكشف≥5، عالِم صغير≥10، بارع الحوار=50 رسالة طالب، قارئ نهم=أول مرفق PDF/DOCX، مصوّر الأسئلة=أول صورة). الأحداث تُطلق من `SessionService` (sendMessage: user_message/vision/document؛ end: session_ended) **best-effort try/catch** — فشل الإنجازات لا يكسر الجلسة أبدًا، والإدراج `onConflictDoNothing` يمنع التكرار عند إعادة التقييم.
5. **العدّادات كلها مُقيَّدة بجلسات الطالب نفسه** (join عبر `learning_sessions`/`messages`/`message_attachments`) — عزل سكوبي لا دفاعي، والطالب يرى نتيجته فقط عبر `GET /api/achievements/me` (تعريفات + `awardedAt` أو قفل)، والوالد/الإدارة مرفوضان 403.
6. **الواجهة**: بطاقة «خطتك» في Home (مجانية/مميزة + انتهاء + حد اليوم + زر «🏆 إنجازاتي»)، شاشة إنجازات تعرض المكتسب والمقفل، وقسم «الاشتراكات» في لوحة الإدارة (قائمة + أزرار ترقية/إلغاء). تُعرض «بلا حدود» عند `dailyLimit === 0`.
**لماذا**: أهم بندَي خارطة MVP المؤجلين (فوترة وإنجازات) كانا «جدولين جاهزين» منذ PHASE 2 — التفعيل منهجي بلا بوابة دفع خارجية (قيود المشروع: أوفلاين حتمي + لا خدمات في الاختبارات)، ويحفّز الطالب بمنحٍ تشغيلية للخطط بلا مخاطرة مالية مبكرة.
**بدائل**: Stripe فوري (خدمة خارجية تكسر الأوفلاين والحتمية — مؤجل لمرحلة إنتاج مستقلة)؛ محفظة نقاط قديمة مؤجلة؛ رفض الوصول عند انتهاء الخطة فورًا (صادم — الهبوط لخطة المجاني أرحم وأوضح في MVP).
**إنتاج لاحقًا**: بوابة دفع (Stripe/Apple/Google)، تجربة/تجديد تلقائي، فواتير، خطط إضافية (family/تعدّد طلاب)، إحصاءات اشتراكات في `/admin/stats`، و«تتابع أسبوعي» من الإنجازات الإضافية — **[نُفِّذ PHASE 32/D-036]** (شارتا «مواظب 3 أيام»/«مواظب أسبوع»)، وبقي منها ربط شارات بمؤشرات إتقان مركّبة.

## D-025 — Qdrant adapter + إعادة تصنيف عبر نموذج (PHASE 21): واجهة VectorStore أصبحت مزوّدًا، وReranker أصبح LLM
**القرار**:
1. **pgvector/Qdrant عبر نفس الواجهة — نفّذنا Qdrant لأن البيئة بلا Docker/Postgres**: واجهة `VectorStore` (upsert/remove/search) موجودة منذ PHASE 5 — PHASE 21 تضيف `QdrantVectorStore` (HTTP صافٍ بلا أي اعتماد جديد: point id **UUID حتمي** من `sha256(chunkId)`، فلتر العزل على **payload النطاق** المُعكَس في كل نقطة، فحص dimension، انتهاء مهلة عبر AbortController) ومصنع `createVectorStore(db, {kind})` يختار من `VECTOR_STORE=sqlite|qdrant` (الافتراضي **sqlite** — صفر تغيير في السلوك/الاختبارات). المخطط قائم: محتوى الـchunks وMetadata في SQLite، والمتجهات في موفّع المتجهات؛ الرجوع لاحقًا إلى Postgres = adapter جديد فقط. **pgvector مؤجل حتى تتوفر Postgres في البيئة** (نفس الواجهة، بلا تغيير في `rag/`).
2. **فشل غير متماثل متعمد**: `upsert`/`remove` ترمي `503 VECTOR_STORE_UNAVAILABLE` (استيراد الإدارة يجب أن يُعلن الفشل؛ وأصبح `Errors.serviceUnavailable` جديدًا في `utils/errors.ts`)، بينما `search` يهبط آمنًا إلى `[]` — **جلسة الطالب لا تنهار أبدًا** إذا كان مخزن المتجهات منقطعًا (المعلم يجيب بلا سياق). التطابق `400 VECTOR_DIMENSION_MISMATCH` يُرفض قبل الوصول للشبكة عند ضبط `QDRANT_DIMENSION`.
3. **إعادة التصنيف أصبح LLM**: واجهة `Reranker` صارت **غير متزامنة**؛ `LexicalReranker` (الافتراضي — سريع وحتمي) بلا تغيير، و`ModelReranker` الجديد يستدعي العملية `"rerank"` (أُضيفت لـ`AIOperation` والـrouter — تُحسب في الاستخدام كبقية العمليات) بطلب JSON صارم، **هبوط آمن**: أي فشل (استثناء مزوّد، ردّ غير JSON، مؤشرات خارج المدى/مكررة) يعيد ترتيب الإدخال كما هو — إعادة التصنيف لا تشوّه الاسترجاع أبدًا؛ الترتيب يطبق على `position` فقط (لا تُلمس درجات المتجه/المعجمي). الاختيار عبر `RAG_RERANKER=lexical|model` و`RAG_ENABLE_RERANK=false` يعطل تمامًا (`NoopReranker` — كان المفتاح المعلّق في env غير موصول، PHASE 21 وصله). **سبب الافتراضي lexicon**: استدعاء LLM في كل استرجاع مكلف وبطيء وغير حتمي — تفعيل النموذج خيار إنتاج.
4. **الاختبار أوفلاين ومباشر على العقد الحقيقي**: `QdrantVectorStore` يُختبر ضد **خادم Qdrant وهمي في العملية** (node:http حقيقي على منفذ عابر — بلا Docker): إنشاء المجموعة تلقائيًا، roundtrip upsert→search مع مراتب cosine، **عزل نطاق عبر فلتر payload**، id حتمي بلا تكرار، remove، هبوط `[]` عند انقطاع الشبكة (عبر `fetchImpl` يرمي ECONNREFUSED)، ورفض mismatch الـdimension (8+1 اختبار). `ModelReranker` يُختبر بمزوّد مقيّد حتمي (7 اختبارات: إعادة ترتيب، مجموعة جزئية، تجاوز استدعاء عند ≤1 مقطع، ردّ غير JSON، مؤشرات غير صالحة، انهيار مزوّد، `parseRanking` صارم). `ragFactory.test.ts` (5) يثبت اختيارات المصنع.
**لماذا**: خارطة RAG الباقية — «مخزن متجهات خارجي + إعادة تصنيف لنموذج» — يمكن الآن تنفيذها أمنيًا (العزل يبقى في الواجهة والقيم الافتراضية) وبتكلفة صفرية على الاختبارات (الكل أوفلاين/حتمي).
**بدائل**: عميل `@qdrant/js-client-rest` (اعتماد جديد يُخالف «صفر اعتماديات» لـ`rag/` وتصعب محاكاته في الاختبارات)؛ محاكاة fakes بعقود زائفة (لا تختبر النقل الفعلي)؛ pgvector حقيقية (تتطلب Postgres — غير متاحة في هذه البيئة، مؤجلة).
**إنتاج لاحقًا**: pgvector adapter (عند توفر Postgres)، مزوّد إعادة تصنيف مخصّص (Cross-Encoder) عبر `AI_RERANK_*`، وفهرسة HNSW معرفة عبر إعدادات Qdrant.

## D-026 — تفعيل التخزين المؤقت (PHASE 22): AiCache يخدم كل العمليات الحتمية، والتكلفة تُسجَّل عند أول استدعاء فقط
**القرار**:
1. **الافتراض الحتمي**: العمليات الحتمية فحسب تُخدم من ذاكرة LRU داخل العملية (أُضيفت عدّادات وعناصر منذ PHASE 6): LLM تعتبرها `classifier` + `rerank` (نفس الرسائل → نفس النتيجة)، و`embedding` (نفس النصوص → نفس المتجهات)، و`ocr` (نفس البايتات → نفس النص — المفتاح `ocr:{model}:{sha256(mime:base64)}`). **المعلّم (`tutor`)/`recap`/`feedback` لا تُخزَّن أبدًا** — تبقى ديناميكية لكل طالب وجولة. مفتاح التخزين = operation+model+بصمة المحتوى (`sha256`) — لا مساحة/بحث في النصوص الحية.
2. **لا يُكتب سجل استخدام عند الضربة**: ضربة الـcache = صفر استدعاء مزوّد = صفر تكلفة = صفر صف في `ai_usage_logs` — الفاتورة تعكس الاستدعاءات الحقيقية. نتيجتان سلوكيتان موثّقتان: (أ) طالبان يرفقان الملف الممسوح نفسه في نفس العملية → الأول يدفع والتعرف يُعاد استخدامه للثاني؛ (ب) استيراد إداري لبايتات ممسوحة سبق التعرف عليها في العملية → بلا شحنة مكررة (تأثير PHASE 22 على `ocr.test.ts` API: الوسم «usage-tracked» صار نسبيًا — النمو 0 أو +1 فقط). مفتاح الإيقاف: `AI_CACHE_ENABLED=false` (افتراضي true) يجعل كل استدعاء git يمر للمزوّد ويُسجَّل.
3. **الإعدادات**: `AI_CACHE_TTL_MS` (5 د) للـLLM، `AI_CACHE_EMBEDDING_TTL_MS` (ساعة — استرجاع نفس نصوث يستمر مرات) و`AI_CACHE_OCR_TTL_MS` (24 ساعة — التعرف ثابت لكل بايتات)؛ وقد أصبح `AiCache` **ملحّيًا بالتنفيذ generic** (ثلاث نسخ: LLM/embedding/OCR) مع `set(key, value, ttlMs?)` اختياري لكل إدخال. العدّادات تُجمَّع في `AiService.cacheStats()` وتُعرض مجانًا على `GET /api/health` (`cache: {hits, misses, size, maxEntries}`) — مراقبة «عدد استدعاءات المزوّد الحقيقي الموفَّرة» بلا أي واجهة جديدة.
4. **الاختبار**: 11 اختبارًا — `unit/aiCache.test.ts` (5 مباشرة: roundtrip+عدّادات، TTL انتهاء، TTL لكل إدخال، إخلاء LRU الأقدم، key محتوى-العنوان + clear) و`unit/aiCaching.test.ts` (6 عبر `AiService` حقيقي بمزوّدات mock: classifier مرّتان → نفس المحتوى + صف استخدام واحد + hits=1/misses=1؛ rerank يُخزَّن؛ tutor مرّتان → صفّان استخدام (لا يُخزَّن) وhits=صفر؛ embed متطابقتان → المتجهات نفسها وhits=1؛ ocr بايتات متطابقة → النص نفسه وصفّ واحد؛ `cacheEnabled:false` → الكل يسجّل). `ocr.test.ts` (API) عُدّل ليعكس تخطّي الشحنة عند البايتات المتطابقة (النمو 0/+1).
**لماذا**: «Caching مُفعَّل» كان آخر بند خارطة — التعبئة الجاهزة منذ PHASE 6 (كل العمليات السابقة كانت تُسجَّل استخدامًا مقابل كل استدعاء)؛ القاعدة الحتمية تجدّد الحقيقي من العمليات الحتمية فقط بلا مساس بحيوية المعلّم، والتكلفة تصير «الدفع عند أول استدعاء فعلي» — عادل وملاحَظ.
**بدائل**: تخزين كل العمليات (يجمّد المعلم = خطأ تربوي)؛ تخزين استمرار عبر Redis/ملف (خارج قيود البيئة — LRU في الذاكرة يكفي لمدة العملية)؛ ختم بلا عدّادات (مراقبة التكلفة عبر `/api/health` أجدى).
**إنتاج لاحقًا**: طبقة تخزين استمرار خارجية (Redis) لـ`AiCache`، إحصاءات «وفورات» في `/admin/stats`، وتسخين مسبق لتعريفات النواة عند الإقلاع.

## D-027 — لوحة ولي الأمر: تفاصيل جلسات الطفل (PHASE 23): خط زمني **بيانات وصفية فقط** — المحتوى لا يُحدَّد ولا يغادر الخادم
**القرار**: تفعيل البند المؤجل صراحةً في TEST_PLAN §4.6 («تفاصيل المحادثة مرحلة مستقلة لاحقًا») كنقطة نهاية قراءة فقط `GET /api/parent/children/:studentId/sessions/:sessionId`:
1. **خط زمني بلا محتوى**: لكل رسالة يُعاد `{id, role, kind, createdAt, safetyFlagged, attachments:[{mimeType, fileName, sizeBytes, itemKind, ocrApplied}]}` فقط — **عمود `content` لا يُحدَّد في الاستعلام إطلاقًا** («لا تُحدّد» أقوى من «لا تُعرض»): لا تسريب بنيويًا، حتى بخطأ برمجي لاحق في التسلسل. المرفقات تُصف بفوق-بيانات (النوع/الحجم/اسم الملف/OCR) بلا بايتات وبلا `sha256` (بصمة محتوى).
2. **المفاهيم والسلامة من مصادر غير المحتوى**: مفاهيم الجلسة من جدول `assessments` (يحمل `sessionId` — تحليل `resultJson` بصبر `safeParseAssessment`)؛ أعلام الحقن من عمود جديد `messages.safety_flag` (`"prompt_injection"`) يُكتب في `sendMessage` عند `intent.intent === "admin_bypass_attempt"` — كان tripwire يُعرض ولا يُحفظ، فأصبح العدّاد مشتقًّا من الصف لا من فحص النصوص. **الـrecap المخزّن لا يُعرض للوالد**: `summarizeSession` يردّد نص الأسئلة حرفيًا («نقاط نوقشت: …») فيكسر قاعدة «لا محتوى خام» — يبقى داخليًا لذاكرة المعلّم طويلة المدى، والترقية لمُلخّص AI آمن للعرض مؤجلة.
3. **العزل بنيوي كما PHASE 18**: لا رابط `students_parents` → 404 بلا مؤشر وجود؛ الجلسة لا تخصّ الطفل المربوط → 404؛ الطالب على `/api/parent/*` → 403. الخصوصية المعلنة للوالد («دون نصوص المحادثات») تبقى صادقة — الشاشة تعرض نشاطًا زمنيًا (متى/ماذا أرفق/كم ردًا/أعلام الأمان) لا كلمات.
4. **الاختبار**: API (2 اختباران إضافيان — `parent.test.ts` 13): خط زمني 4 أدوار بدقة مع مرفق صورة + عَلَم حقن واحد + مفهوم من `recordAssessment`، و**6 نفي تسريب** (نصّا السؤال والرد، عبارة المعلّم، بايتات الصورة، غياب حقل `content`)؛ عزل 403/404 ×٤. E2E P4 (واحد): الطالب يسأل + حقن → الوالد يفتح التفاصيل → 4 مداخل + شارة أمان + 3 نفي تسريب.
**لماذا**: البند الوحيد الباقي الموثّق كمؤجَّل منذ PHASE 18 وقابل للبناء أوفلاين بلا اعتماديات؛ «تفاصيل المحادثة» لا تعني بالضرورة النصوص — قيمة الوالد الحقيقية (كيف مضى وقت طفلي؟) تُلبَّى بفوق-البيانات، والضمان الأقوى هو عدم تحديد المحتوى أصلًا.
**بدائل**: عرض الـrecap للوالد (مرفوض — يعيد نص الأسئلة)؛ عرض المرفقات كاملة (مرفوض — بايتات/محتوى أصلًا)؛ تخزين الأعلام خارج الرسائل (جداول إضافية بلا قيمة — عمود واحد كافٍ).
**إنتاج لاحقًا**: مُلخّص جلسة AI آمن للعرض (عملية `recap` مع فحص «لا نص حرفي»)، تنبيهات دفع للوالد عند علم أمان جديد، وتصدير تقرير أسبوعي PDF.

## D-028 — محرك إتقان المفاهيم (PHASE 24): إتقان يُحسب قراءةً + تفعيل جداول التمارين الخاملة كقناة تقييم حقيقية
**القرار**: بناء «محرك إتقان» فوق `student_progress`/`assessments` الموجودين وتفعيل جداول `questions`/`answers` (جاهزة في المخطط منذ PHASE 1 لكنها خاملة — لم يستدعِ `recordAssessment` أيّ مسار إنتاجي):
1. **إتقان محسوب قراءةً، لا كتابةً**: `MemoryService.masterySummary` يزيّن كل مفهوم بـ `{mastery (خام)، decayedMastery (بعد انحلال أُسي Ebbinghaus — `MASTERY_DECAY_PER_DAY` افتراضي 0.02 ≈ نصف عمر 35 يومًا)، level (متقن ≥0.8 / متقدم ≥0.6 / قيد التقدم ≥0.4 / يحتاج مراجعة <0.4)، labelAr، daysSinceLastPractice، trend (up/steady/down من آخر ≤3 تقييمات مقابل ما قبلها)}`. **الانحلال قراءةً فقط** — ذاكرة المعلّم طويلة المدى (prompt) تستمر بالقيمة الخام، وعرض الطالب/الوالد يرى الإتقان الواقعي. المعامل الجديد `MASTERY_DECAY_PER_DAY` في env (0 = إيقاف).
2. **محرك نقي** (`progress/mastery.ts`، بلا I/O واختباره كامل الوحدات) يوحدّ التعريف بين كل الأسطح (لوحة الطالب، الوالد، تغذية التمرين) + نقل `safeParseAssessment` إليه مركزيًا (كان منسوخًا محليًا في parent/service).
3. **التمرين قناة التقييم**: `GET /api/practice/question` (أضعف المفاهيم المتتبعة أولًا ثم أي سؤال ضمن مناهج الطالب المسجَّلة، **بلا `correctIndex`/`answerKey` في الاستجابة إطلاقًا**) و `POST /api/practice/questions/:id/submit` — تصحيح حتمي (MCQ، صفر استدعاءات AI → أوفلاين وبلا تكلفة) ثم كتابة `answers` + `recordAssessment(type:"exercise")` + رد بشرح ومستوى محدَّث. عزل: غير مسجَّل في منهج السؤال → 404، ولي أمر/غير طالب → 403، مؤشر خيار خارج النطاق → 400.
4. **بذر أسئلة تجريبية idempotent** (فرعا البذر: الجديد والموجود): 6 MCQ مصرية مربوطة بمفاهيم الدروس الثلاثة بصيغة `{options, correctIndex}` داخل `optionsJson`. **السعودية بلا أسئلة عمدًا** — إثبات أن نطاق التمرين فعلًا لكل منهج (الطالب السعودي المسجَّل في منهجه لا يرى أسئلة مصر).
5. **الاختبار**: وحدة `mastery.test.ts` (15: حدود المستويات، انحلال/تعطيل، اتجاهات، tolerances) + API `practice.test.ts` (8: أضعف أولًا + لا تسريب، تصحيح صحيح/خاطئ يسجّل ويعكس على `studentProgress`/`answers`/`assessments`، `progress/me` يكشف `mastery`، 403/404/400) + E2E `practice.spec.ts` PR1 (تمرين → تغذية راجعة + شارة مستوى → reload → صفّ إتقان).
**لماذا**: الإتقان هو قلب القيمة التربوية («المدرس الخصوصي الذكي» يعرف ماذا يتقن الطالب ومتى ينحسر)؛ الجداول كانت جاهزة وبلا مستهلك — تفعيلها بلا مخطط جديد يغلق الحلقة التربوية (ضعف → ممارسة → إتقان) بأرخص مكوّن ممكن (تصحيح حتمي بلا AI).
**بدائل**: ربط إشارات «فهمت/مش فاهم» كتقييمات (مرفوض — إشارات ضعيفة وغير دقيقة تُلوّث الإتقان)؛ تصحيح الأسئلة المفتوحة بالـAI (مؤجل — يتطلب عملية LLM وتكلفة؛ MCQ يغطي MVP)؛ كتابة الانحلال على `studentProgress` بحراسة زمنية (أثقل — حِمل كتابة يومي بلا فائدة: عرضي فقط).
**إنتاج لاحقًا**: تصحيح أسئلة مفتوحة بنموذج + أسئلة مولّدة بالمفهوم (LLM)، توصيات ممارسة منفّذة كخطة (من الإتقان إلى قائمة دروس/تمارين — **نُفِّذت PHASE 25/D-029**)، المعادلة تعتمد على صعوبة السؤال ومدته (**نُفِّذت PHASE 26/D-030**)، وربط مخرجات المحرك بشارات الإنجاز (مثلًا «متقن 5 مفاهيم» — «أول إتقان» **نُفِّذ PHASE 26/D-030**، وتدرّج «متقن 3/5 مفاهيم» وزمن الإجابة **نُفِّذا PHASE 31/D-035**).

## D-029 — خطة الممارسة (PHASE 25): تحويل الإتقان إلى خطة مرتّبة قابلة للتنفيذ (أضعف المفهوم أولًا)
**القرار**: استيفاء البند المؤجل من D-028 («توصيات ممارسة منفّذة كخطة») كقراءةٍ نقيّة: `GET /api/practice/plan` يبني من `masterySummary` خطة مرتّبة بلا أي كتابة — كل مفهوم متتبَّع يظهر مع درسه وعدد الأسئلة المتاحة:
1. **ترتيب «الأضعف أولًا» بقاعدة صافية** (`practice/plan.ts` — `sortPlan` نقي واختباره وحدات): (1) `decayedMastery` تصاعديًا — تمارين على ما تعرفه أقل الآن؛ (2) `daysSinceLastPractice` تنازليًا — المهمل منذ زمن يتقدّم في الصف؛ (3) `conceptId` — كسر تعادل حتمي للاستقرار. المحرك يحسب الانحلال **نفسه** في كل الأسطح (خطة = لوحة الطالب = إتقان الوالد).
2. **العدّاد `availableQuestions` يحترم نطاق التسجيل**: يعدّ فقط أسئلة MCQ ضمن مناهج الطالب المسجَّلة (`curriculumEnrollments` النشطة) — مفهوم متتبَّع لطالب غير مسجَّل = 0 (يظهر «بلا أسئلة» ويُعطَّل زر التمرين، لا يُخدَع الطالب بوعد تمرين مغلق بلا مادة).
3. **قراءة نقية بلا مفتاح**: الخطة فوق-بيانات (مستوى/نسبة/اتجاه/درس/عدد) — لا خيارات ولا `correctIndex`؛ مسار `/plan` بنطاق student مثل بقية التمرين (وليّ أمر → 403، غير مصادق → 401).
4. **الويب**: قسم «خطة ممارستك» داخل بطاقة التقدم — صف لكل مفهوم (شارة مستوى + درس + «n سؤال متاح» + حداثة + سهم اتجاه) وزر «تمرّن الآن» يفتح لوحة التمرين **مقيّدة بالمفهوم** عبر `conceptId` (يعيد استخدام `GET /practice/question?conceptId=` — لا مسار جديد للأسئلة)؛ تُحدَّث الخطة بعد كل إجابة وإغلاق.
5. **الاختبار**: وحدة `practicePlan.test.ts` (5: أضعف أولًا، كسر تعادل بالحداثة، كسر نهائي بالمعرف، لا تعديل للمدخلات، متقن بعد ضعيف) + API `practicePlan.test.ts` (5: خطة فارغة بلا تتبع، أقوى مفهوم أولًا بدرس + عدد أسئلة + لا تسريب مفاتيح، ترتيب أضعفين، عدّاد النطاق 0 لغير المسجَّل، 403 للوالد) + E2E `practice.spec.ts` **PR2** (خطة ≥1 صف بدرس/عدد/شارة/اتجاه → «تمرّن الآن» لأول مفهوم ممارَس يفتح لوحة التمرين بسؤال حقيقي).
**لماذا**: الإتقان بلا خطة قيمة مجمّدة — «المدرس الخصوصي» لا يعرض البيانات فقط بل يوجّه «الآن تمرّن هنا»؛ القاعدة صافية ومُختبَرة فتبقى التجربة متّسقة بين الأسطح، والتمرين المقيّد بالمفهوم يرمم دورة «اكتشاف الضعف → معالجته» مباشرة.
**بدائل**: خطة مولّدة بالـAI (مؤجل — تكلفة ولا حاجة: الترتيب قاعدة حتمية أوضح وأرخص)؛ دمج الخطة داخل `progress/me` (مرفوض — `progress/me` لا يحتاج عدّاد أسئلة العزل، والانفصال يبقي `plan` بطيئًا عند الحاجة فقط).
**إنتاج لاحقًا**: المعادلة تعتمد على صعوبة السؤال ومدته (**نُفِّذت PHASE 26/D-030**)، أسئلة مولّدة بالمفهوم (LLM) لتغطية المفاهيم بلا أسئلة، وربط الخطة بشارات الإنجاز (مثلًا «متقن 5 مفاهيم» استكمالًا لـD-028 — **نُفِّذ PHASE 26/D-030**).

## D-030 — حساسية الصعوبة + شارات الإتقان (PHASE 26): معادلة الإتقان تستجيب لصعوبة السؤال، والمحرك يغذّي شارات الطالب
**القرار**: إغلاق بندي D-028 المؤجلين معًا بلا اعتماديات: (أ) **المعادلة تعتمد على صعوبة السؤال** — دلتا EWMA في `recordAssessment` تُشتق من `questions.difficulty` عبر `assessmentDelta` النقّي في `mastery.ts`: سهل +0.15/−0.1 (الحسم التاريخية بدقة — لا تغيّر على المسار القديم)، متوسط +0.175/−0.125، صعب +0.2/−0.15؛ **المحاولة الأولى تبقى محايدة للصعوبة** (صحيح 0.6 / خاطئ 0.1) فالبداية تثبت التوقع، والتفاضل يظهر على التتابع؛ (ب) **ربط المحرك بشارات الإنجاز** — تعريفان جديدان في `AchievementService` (المجموع 6 → 8): `practice_starter` «انطلاقة التمرين» (أول إجابة) و`mastery_first` «أول إتقان» (أول مفهوم عند مستوى «متقن» — يُحتسب بالدرجة المعروضة `decayMastery` نفسها التي يراها الطالب لا الخام).
1. **دلتا نقية موحّدة**: `assessmentDelta(difficulty?)` في `mastery.ts` (افتراضي easy) — يُستدعى من `recordAssessment`؛ فحوص الدرس (`concept_check` بلا صعوبة) تبقى كما كانت حرفيًا؛ تمرين `submit` يمرر `q.difficulty` الفعلي.
2. **الشارات عبر نفس مسار الإنجازات**: `PracticeService.submitAnswer` بعد التقييم يستدعي `achievements.evaluate` لحدثَين — `practice_answer` و`mastery_achieved` — **best-effort داخل try/catch** (محاسبة الشارات لا تكسر حلقة الإجابة أبدًا، وأي فشل يُعاد على الإجابة التالية). العدّادان في `countForEvent`: عدد صفوف `answers`، وعدد المفاهيم ذات المستوى المعروض «متقن». العزل التلقائي: `evaluate` يسأل `studentProgress`/`answers` للطالب نفسه فقط (طالب زميل بلا نشاط = كلاهما مقفول).
3. **لا تغيير ويب**: الشاشتان تعرضان كل التعريفات (مكتسبة + مقفولة) تلقائيًا — الشارتان تظهران فور الجلسة الأولى للكل، وتُقفل حتى يحققها الطالب.
4. **الاختبار**: وحدة `difficultyDelta.test.ts` (6: قيم الدلتا الثلاث صحيح/خاطئ، الرجوع لـeasy عند الغياب، الإشارات، التراتب القوي hard>medium>easy) + API `practiceDifficulty.test.ts` (3: المحاولة الأولى 0.6 محايدة، الثانية الصحيحة على سؤال صعب → 0.8 (+0.2)، الثالثة الخاطئة → 0.65 (−0.15)) + API `practiceBadges.test.ts` (4: الاثنتان مقفولتان قبل أي تمرين، الأولى تجلب «انطلاقة التمرين» (نتيجة صحيحة أو خاطئة على السواء)، «أول إتقان» بعد الوصول لـ0.85 فعليًا على نفس المفهوم، وعزل الشارة بين طالبين) + تحديث ميكانيكي لاختبار PHASE 20 (`total` 6 → 8) + E2E `practice.spec.ts` **PR3** (بعد إجابات PR1/PR2: «انطلاقة التمرين» مكتسبة و«أول إتقان» مقفولة في شاشة الإنجازات).
**لماذا**: «المدرس الخصوصي» يقيس إتقانًا ويعاقب/يكافئ بحسب صعوبة ما يُسأل عنه — وحدة اختبار ANSWER بعد سؤال صعب دليل أقوى من سهل؛ وبإغلاق الحلقة لابيات: إتقان → خطة → ممارسة → **شارة** يرى الطالب تقدّمًا ملموسًا فور إجابته الأولى بلا أي شاشة جديدة (إعادة استخدام كاملة لمنظومة PHASE 20).
**بدائل**: دلتا موحّدة لكل الأسئلة (مرفوض — أعمى بصريًا عن جودة السؤال؛ حفظنا الافتراضي لكن فعّلنا التفاضل)؛ ربط شارات الإتقان بالمستوى الخام بدل المعروض (مرفوض — الشارة يجب أن تطابق ما يراه الطفل «متقن»، والانحلال يُحتسب عند الامتدادات)، شارة جديدة بمستوى أدنى من «متقن» (مؤجل — إغراق).
**إنتاج لاحقًا**: معادلة تشمل وقت الإجابة (السرعة مؤشر إتقان — **نُفِّذ PHASE 31/D-035**) وتدرّج شارات إتقان أعلى («متقن 3 مفاهيم»/«5» — **نُفِّذ PHASE 31/D-035**: يعمل بلا انتظار بنوك أوسع، يكفي إتقان مفاهيم متعددة)، أسئلة مولّدة بالمفهوم (LLM) لتغطية المفاهيم بلا أسئلة (**نُفِّذت PHASE 28/D-032**).

## D-031 — إحصاءات الاشتراكات + وفورات الكاش (PHASE 27): اكتمال لوحة المدير بقراءةٍ نقيّة تُغلق بندي D-024 وD-026 المؤجلين
**القرار**: توسيع `GET /api/admin/stats` — بنفس نمط PHASE 17 (تجميعات فوق الجداول الحالية، بلا تخزين جديد، admin-only) — بقسمين: (أ) **قمع الاشتراكات** (`subscriptions{total, free, premium, active, conversionRate}` — توزيع الخطة المخزّنة + «المميزة السارية فعليًا» = premium و(status trialing|active) وغير منتهية + نسبة تحويل بأعشار %؛ الأرقام تتطابق مع شاشة الاشتراكات الإدارية)، (ب) **الاستخدام + الوفورات** (`ai{calls, byOperation, tokens, costUsd, cache{hits,misses,hitRate,size,maxEntries}, estimatedSavingsTokens, estimatedSavingsUsd}` من `aiUsageLogs` و`ai.cacheStats()` — عدّادات فقط، بلا محتوى رسائل أبدًا).
1. **تقدير وفورات صادق**: كل إصابة كاش تخطّي نداء مزوّدٍ بلا صف استخدام — نقيس متوسط التوكن/التكلفة على نداءات العمليات **القابلة للتخزين** (classifier/rerank/embedding/ocr) التي وصلت فعلًا للمزوّد (المسجَّلة)، وعند غيابها (مثل Embeddings في mock تُخزَّن ولا تُسجَّل) نستعمل متوسط المنصة كبديل حتى يبقى الرقم ذا معنى؛ صفر في mock لا يعكس خللًا — التسعير صفر.
2. **ويب بلا اعتماديات**: بطاقتان في لوحة الإدارة — «الاشتراكات — نظرة سريعة» و«الذكاء الاصطناعي — الاستخدام والوفورات» — من `AdminStats` الموسّع في `api.ts`؛ لا بيانات خارج ما يراه المدير أصلًا.
3. **الاختبار**: API `adminStatsExtended.test.ts` (4: قمع free/premium/active/تحويل 50% بعد ترقية مميزة سارية، انحدار past_due → «سارية» 0 مع بقاء الخطة المخزّنة premium، عدّادات الاستخدام (calls/tokens/tutor≥2) بعد دورتين، إصابات كاش بعد تكرار نفس السؤال مع وفورات توكن >0 وUSD=0 على mock) + E2E `admin.spec.ts` **A4** (بطاقتا القسمين تعرضان للمدير).
**لماذا**: لوحة المدير هي «لوحة عدادات المنصة» — الاشتراكات والوفورات هما المؤشران اللذان يقرأهما صاحب المنتج يوميًا (من يدفع؟ وما الذي يوفّره الكاش؟)؛ وكلاهما كان مؤجلًا من PHASE 20/22 بانتظار هذه الجلسة، والبناء فوق `/admin/stats` الحالي يحافظ على توقّعٍ واحد للمدير.
**بدائل**: عدّادات كاش مجمّعة بلا تقدير نفقة (مرفوض — بلا بوصلة مالية)؛ قسم منفصل `/admin/analytics` (مؤجل — شاشة واحدة تكفي في MVP)؛ إحصاءات اشتراكات داخل شاشة الاشتراكات الموجودة (مؤجل — الأرقام التجميعية تعيش بجوار بقية العدّادات).
**إنتاج لاحقًا**: تقسيم الفترة الزمنية (اليوم/الأسبوع) للاستخدام والوفورات، معادلة وفورات تشمل وقت التأخير (latency)، وقراءة من بوابة الدفع الحقيقية عند وصولها.

## D-032 — أسئلة مولّدة بالمفهوم (PHASE 28): تغطية المفاهيم بلا أسئلة عبر عملة LLM `question_gen` مؤجّلة من D-028/D-029/D-030
**القرار**: إغلاق البند المؤجل المتكرر «أسئلة مولّدة بالمفهوم (LLM) لتغطية المفاهيم بلا أسئلة» — عملة AI جديدة قابلة للتخزين (حتمية عبر mock) تولّد **سؤال MCQ واحدًا لكل مفهوم بلا أسئلة**، مرتكزًا على مقاطع درس المفهوم (السقف `QUESTION_GEN_MAX_CHUNKS`=6 / `QUESTION_GEN_MAX_CONTEXT_CHARS`=4000)، بلا مخطط جديد: السؤال المولّد يُخزَّن في جدول `questions` الحالي (نوع `mcq`، صعوبة `easy`، `optionsJson={options, correctIndex}`، `answerKey=null`) — فيدخل مباشرة في حلقة «الضعف → ممارسة → إتقان» الموجودة (يُصحَّح ويتدرّب تقييمًا كما أي سؤال مزروع).
1. **مسار الطالب (توليد ذاتي يرمم الخطة)**: `POST /api/practice/generate {conceptId}` (student فقط، 404 لمفهوم مجهول أو خارج مناهج الطالب المسجَّلة، 409 إذا كان للمفهوم أسئلة MCQ بالفعل، 503 عندما يكون درس المفهوم بلا مقاطع / فشل `parseGeneratedQuestion`)؛ الاستجابة `{question}` عامة (بلا مفتاح كالعادة). في الويب يتحول صف خطة بلا أسئلة (المفاهيم غير المتتبعة الآن **تُدرَج** في `GET /api/practice/plan` بدرجة إتقان 0 و`tracked:false` مرتبة بعد المتتبعة — كانت مخفية) إلى زر «توليد سؤال» (`plan-generate`) يولّد ثم يفتح لوحة التمرين فورًا.
2. **مسار الإدارة (تغطية جماعية)**: `POST /api/admin/questions/generate` بقيمة نطاق واحدة `{conceptId|lessonId|curriculumId}` → `{result: {generated, skipped, failed, items}}` (بيانات وصفية فقط بلا أي محتوى أسئلة)، **idempotent** (`countMcqForConcept` يتخطى المغطّى — إعادة التشغيل تولّد 0)، والمفاهيم بلا مقاطع تُبلَّغ `failed`؛ يُسجَّل في سجل التدقيق `question.generate`؛ العملية `question_gen` أُضيفت إلى `LLM_CACHEABLE` و`CACHEABLE_OPERATIONS` (إحصاءات PHASE 27 تحسبها أوفلاين) — فلا شحنة مزدوجة والوفورات أصدق.
3. **البناء الجوهري**: `parseGeneratedQuestion` (حد صارم: JSON بعد تجريد ```` ```json ````، نص ≥5 خانات، خيارات ≥4 غير فارغة، `correctIndex` عددي في المدى، شرح اختياري → null) + `groundingPrompt` (`<context>` + «المفهوم: «…»») + mock حتمي `buildMockQuestion`: الحقائق من تقسيم السياق على `[.!؟؛\n]` مع استبعاد جُمل النفي التربوي («غير موجود» — حاجز حتمي بلا مقابل للـtripwire)، seed `cyrb128` + دالة تجزئة → السؤال الصحيح **حرفي من الدرس**، مشتّتات = طفرة رقم + حشوات، وتدوير مقدار حتمي لإخفاء موضع الصحيح. **بلا تسريب للمفتاح**: `correctIndex` لا يُعرض للطالب/الويب أبدًا (نفس قاعدة PHASE 24 — يُقرأ خادميًا عند التصحيح فقط).
4. **الاختبار**: وحدة `questionGenMock.test.ts` (**14**: حتمية، تأصيل حرفي، نفي «لا غير موجود» كإجابة، تغذية راجعة، fallback، `<<parser` حدود 7، `<<groundingPrompt`) + API `practiceGenerate.test.ts` (**9**: 403 والد، 400 بلا conceptId، 404 مجهول/خارج نطاق، 409 مغطّى، توليد+تخزين بنطاق المنهج ولا تسريب، تصحيح السؤال المولّد + تحديث إتقان عبر `correctIndex` من DB، 503 بلا مقاطع، 401) + API `adminQuestionGen.test.ts` (**6**: 403 طالب، 400 نطاق 0/2، نطاق درس → ولّد 1 وتخطّى 2، نطاق منهج → ولّد 1 وتخطّى 4 مع تخزين `easy`، إعادة تشغيل idempotent (0/5)، فشل بلا محتوى) + تحديث عقد PHASE 25 (خطة = 3 صفوف والمفاهيم غير المتتبعة `tracked:false`؛ صفوف `plan` صارت تُدرج غير المتتبع) → **289/289 أخضر**؛ E2E **A5** (الإدارة تولّد للمنهج المصري: «تم توليد 1 سؤالًا» — مقارنة الكسور الوحيد بلا أسئلة) و**PR4** (الطالب يمرّن سؤال مقارنة الكسور المولّد: «أي العبارات التالية وردت في الدرس» → إجابة + تغذية + إتقان بعد reload) → **36/36 أخضر**.
**لماذا**: «مدرس خصوصي ذكي» لا يترك مفهومًا بلا مادة — المفاهيم بلا أسئلة (كما «مقارنة الكسور» المصرية والسعودية كلها) كانت **غير قابلة للاكتشاف** في الخطة (مرتبة بخفاء) رغم وجود مقاطع درس؛ التوليد يعيد إليهما القيمة ويغلق الحلقة التربوية للبنوك غير المكتملة أوفلاين (مسار D-028 المعرقل الوحيد المتبقي).
**بدائل**: بنوك أسئلة يدوية لكل المفهوم (مرفوض — عبء تحريري وكل مفهوم جديد يفتح ثغرة جديدة)؛ توليد عند **طلب الطالب فقط** بلا مسار إداري (ناقص — الإدارة تحتاج تغطية جماعية للبنك)؛ `question_gen` خارج `LLM_CACHEABLE` (مرفوض — إعادة التوليد لنفس المفهوم بلا تغيير مقاطع يجب ألا تُحصى شحنتين).
**إنتاج لاحقًا**: أسئلة مفتوحة تُصحَّح بنموذج (LLM) — المتبقي الوحيد من D-028 (**نُفِّذت PHASE 30/D-034**)، ومعادلة إتقان تشمل وقت الإجابة (**نُفِّذت PHASE 31/D-035**)، وبطاقات أسئلة أكبر عبر مقاطع دروس متعددة.

## D-033 — ملخص الجلسة الآمن (PHASE 29): إغلاق «مُلخّص جلسة AI آمن للعرض (عملية `recap` مع فحص «لا نص حرفي»)» المؤجل من D-027
**القرار**: ملخص **آمن للعرض** بعد كل جلسة — يقرؤه الطالب ووليّ أمره المرتبط — عبر عملية LLM `recap` (ديناميكية، خارج `LLM_CACHEABLE` مثل tutor/feedback) تُبنى من **بيانات وصفية فقط** (عنوان الدرس، أسماء المفاهيم، وأعداد فقط: رسائل الطالب/المدرس، المدة، المرفقات، أعلام الأمان) — محتوى الرسائل **لا يدخل الاستدعاء ولا النتيجة**: «لا تسريب» بالتصميم. حارس **«لا نص حرفي»** `recapContainsMessageContent` يرفض أي ناتج يعيد إنتاج رسالة (باتجاهين، مع استبعاد عناوين الدرس/المفاهيم كرموز مسموحة حتى لا يُطلق حارس على طالب كتب العنوان نفسه)، وسقوط آمن حتمي `buildRecapFallback` (`fallback:true`) — فيرى المستخدم دائمًا ملخصًا آمنًا. صفر مخطط جديد.
1. **الوجهان**: الطالب `GET /api/sessions/:id/recap` (student فقط؛ غير طالب → 403؛ جلسة الآخرين → 403 ملكية عبر `getOwned`؛ غير موجودة → 404؛ جلسة بلا رسائل → `{recap:null}`)؛ الوالد `GET /api/parent/children/:studentId/sessions/:sessionId/recap` (بوابة `students_parents` → 404 قبل أي قراءة، نفس الحمولة، ويُسجَّل `contextUserId` لولي الأمر — `ai_usage_logs.userId` يرجع لـ`users` لا `students`). الاستدعاء `json:true` يُسجَّل استخدام `operation="recap"` (ديناميكي — لا يُخدم من الكاش).
2. **البناء**: `sessions/recap.ts` (جديد): `SessionRecap`/`SessionRecapMetadata` + `buildRecapMetadataBlock` (`<metadata>` لا يتضمن إلا العنوان/المفاهيم والأرقام) + `parseRecap` (حد صارم: JSON بعد تجريد ```json، عنوان ≥5، تركيز ≥3، قوائم سلاسل ≤4 نقاط) + `recapContainsMessageContent` (اتجاهان: رسالة كاملة ≥12 حرفًا داخل الملخص، وجملة ملخص ≥25 حرفًا داخل رسالة — بعد استبعاد العناوين) + `buildSessionRecap`/`buildRecapFallback`؛ mock `extractMetadataBlock` + `buildMockRecap(block)` حتمي (قوالِب تُنتقى من العدادات — بلا محتوى)؛ `SessionService.recap(sessionId, studentId, actorUserId)` و`ParentService.sessionRecap` (حقن `sessions` في الوالد).
3. **الويب**: مكوّن مشترك `web/src/RecapCard.tsx` — في Home زر «ملخص الجلسة» على صف الجلسة المنتهية بالذات (القائمة تصاعدية → استهداف الصف بمعرّفه `data-session-id`) يفتح البطاقة (عنوان/تركيز/إحصائيات/نقاط قوة/اقتراحات/تنبيه `recap-fallback`)، وفي Parent بطاقة «ملخص الجلسة الآمن» (بعد مفاهيم الجلسة) — كلاهما يعرض **نفس الحمولة** فلا يرى الوالد أكثر مما يرى الطالب.
4. **الاختبار**: وحدة `recap.test.ts` (**18**: كتلة وصفية بلا رسائل، حدود الباحث 5، حارس 5 (اتجاهان + استثناء العنوان + رسالة قصيرة داخل ملخص أطول)، دمج ناجح + سقوطان (غير JSON/تسريب)، حتمية fallback وتلميح أمان، موك حتمي قابل للتحليل) + API `sessionRecap.test.ts` (**8**: حتمية لنفس الجلسة + عنوان درس في العنوان، لا رسالة سرية في العرض، null بلا رسائل، عزل طالب 403، غير طالب 403، ولي أمر مربوط/غير مربوط 404/403 على مسار الطالب) → **315/315 أخضر**؛ E2E `recap.spec.ts` **R1** (طالب: يتعلم → يرسل رسالة سرية → ينهي → «ملخص الجلسة» على صفّه → العنوان يحمل درسَه ولا نص الرسالة) و**R2** (والد: يفتح أحدث جلسة → «عرض الملخص» → نفس الضمانة) → **38/38 أخضر**.
**لماذا**: «مدرّس خصوصي ذكي» لا ينهي درسًا بلا محصّلة — الطالب يحتاج تشخيصًا ذاتيًا سريعًا والوالد (بلا رفع أي نصوص) يحتاج رؤية تقدّم طفله؛ كان العرض الآمن مؤجلًا في D-027 لأن «اطلب من النموذج ألا يعيد النص» بلا ضمان، بينما البناء **الوصفي-فقط + حارس الحرفية + سقوط حتمي** يجعلان الضمانة بنيوية ويظلان أوفلاين بالكامل.
**بدائل**: عرض ملخص الذاكرة الداخلية `session_recaps` (مرفوض — يُبنى من نصوص الرسائل «نقاط نوقشت: …» وهو لاستهلاك تلميح المعلّم لا للعرض)؛ إرسال نص الرسائل للموديل «مع طلب عدم إعادتها» (مرفوض — لا ضمان، يكسر وعد الخصوصية)؛ توليد عند الإنهاء وتخزينه في الجدول (مؤجل — عمود/جدول جديد وتكلفة شحنة لكل جلسة ولو لم تُعرض؛ عند-الطلب حتمي مع mock وبلا مخطط: نفس الشحنة تُحصى عند العرض فقط).
**إنتاج لاحقًا**: ملخص مخزَّن عند الإنهاء (البُذر/إعادة الاستخدام — لازم جدول/عمود)، نسخ مخصصة للدور (طالب/والد/معلّم)، وأرشيف ملخصات قابلة للتجميع.

## D-034 — التصحيح الآلي للإجابات المفتوحة (PHASE 30): تفعيل أسئلة `type:"open"` الخاملة عبر عملية LLM `grade_open` — إغلاق آخر بند مؤجل في D-028
**القرار**: تفعيل الأسئلة **المفتوحة** (إجابة حرة) وإغلاق آخر بند مؤجل من D-028 — جدول `questions` يحمل منذ PHASE 24 عمود `answerKey` الخامل (للإجابة النموذجية المخبأة)؛ الآن يُفعَّل: `toPublic` يفرّق بطبقة النوع (`open` → `options:null,type:"open"` و`mcq` → `options:string[]`)، وكل سؤال مفتوح يُصدَّر مع **مفتاحه النموذجي خادميًا فقط** (`answerKey` لا يغادر الخادم إطلاقًا كما `correctIndex`). تصحيح الإجابة الحرة عبر عملية LLM **ديناميكية** جديدة `grade_open` (تُسجَّل استخدامًا و**خارج `LLM_CACHEABLE`** — كل تصحيح جديد يُكلَّف، وقائمة `CACHEABLE_OPERATIONS` الإدارية لم تتغيّر) تُبنى تلقائيًا من حقلين مفعّلين على صف السؤال: `<reference>` في رسالة النظام (الإجابة النموذجية) و`<student_answer>` في رسالة المستخدم — فالإجابة المفتوحة لا تدخل رسالة النظام إطلاقًا. **سقوف صارمة**: `GRADE_OPEN_MAX_STUDENT_ANSWER_CHARS=1500` (فوقها `400 INVALID_ANSWER`) و`GRADE_CORRECT_THRESHOLD=0.7`/`GRADE_CLOSE_THRESHOLD=0.4`.
1. **عزل الإجابة النموذجية**: `practice/grade.ts` (جديد) — `normalizeArabic` (تجريد شكّل/تطويل، توحيد أشكال الألف والياء، تحويل الأرقام العربية/الهندية إلى لاتينية، إطباق فراغات) + `tokensOf` (تقسيم على غير حرف/رقم — «والمقام» رمز واحد) + `referenceCoverage` (نسبة توكنات المفتاح التي غطّتها الإجابة؛ النصوص القصيرة الرقمية معاملة تطابق تام) + `gradePrompt` (يفصل `<reference>`/`<student_answer>` سابقًا أعلاه) + `parseGrade` (حد صارم: JSON بعد تجريد ```json، `correct` منطقي، `score` عددي ∈[0,1]، `feedback` غير فارغ) + **حارس «لا نص حرفي»** `gradeContainsAnswerKey` (رفض تغذية راجعة تعيد إنتاج المفتاح نصًا حرفيًا ≥3 أحرف أو ≥60% من توكنات المفتاح) + **سقوط حتمي** `gradeFallback` (قَوالب ثابتة تصف الحال «إجابة موفقة/قريبة/غير دقيقة» **بلا إعادة نص المفتاح أبدًا**).
2. **المزوّد mock**: فرع `grade_open` يقرأ `<reference>` من النظام و`<student_answer>` من آخر رسالة مستخدم عبر `extractBlock` + `buildMockGrade(reference, studentAnswer)` حتمي = **تصحيح تغطية التوكنات** (صحيح ≥0.7، قريب ≥0.4، وإلا ضعيف — بتقدير `score` وتغذية راجعة قَالَبية ثابتة بلا نص المفتاح) — التطابق التام يعطي المعادلة النموذجية نفس نتيجة mock. `question_gen` تعرف النوع (رسالة المستخدم تحمل «النوع: mcq|open») + `buildMockOpenQuestion(context, conceptTitle)` = سؤال مقالي يطلب **حقيقةً واحدة وردت حرفيًا في الدرس** (نفس اختيار المقطع الحتمي بـ`cyrb128` مع استبعاد النفي «غير موجود») ومفتاحها النموذجي **لا يُقتبَس في التوضيح**.
3. **الخدمة والشبكة**: `service.ts` يوزّع تحت `submitAnswer` → `gradeMcqAnswer` (كالمعتاد) / `gradeOpenAnswer` (استدعاء `grade_open` بـ`contextUserId=actorUserId`، `answerKey` من قاعدة البيانات فقط، صف `answers` بـ`content` = الإجابة الحرة، تقييم `recordAssessment` يعتمد `grade.correct`؛ إجابة فارغة/أطول من السقف → `400 INVALID_ANSWER`، مفتاح مفقود على صف مفتوح → `Errors.internal`)؛ توليد بنوعين: `POST /practice/generate {conceptId, kind:"mcq"|"open"}` (حارس **عداد لكل نوع** → `409` للمغطّى من نوعه) و`kind?` في توليد الإدارة الجماعي؛ `questionFor` يخدم `?type=mcq|open`؛ `planFor` يضيف **`openQuestions`** لكل مفهوم. رموز خطأ جديدة: `INVALID_TYPE`/`INVALID_KIND`/`INVALID_SUBMIT`/`INVALID_ANSWER` (مع بقاء `INVALID_OPTION` للمطابقة).
4. **البذر الحتمي E2E**: سؤالان مصريان مفتوحان — «تبسيط الكسور» (`answerKey` نص «2/3» على درس الكسور) و«القسمة المطولة» (`answerKey` «26»)؛ السعودية تبقى بلا أسئلة تجريبية. صف الخطة يكتسب زر **«سؤال مقالي»** (`openQuestions>0`) أو **«توليد سؤال مقالي»** وإلا، ولوحة التمرين تعرض حقل نص حر (`practice-open-input`) وتغذية راجعة مطابقة (نص `feedback` + شارة `score`% + إتقان كالمعتاد) — **والطالب لا يرى المفتاح ولا النص النموذجي في أي استجابة**.
5. **الاختبار**: وحدة `grade.test.ts` (**27**: تطبيع/توكنات/تغطية/حد `parseGrade`/فصل القالب/حارس بمفتاح قصير وطويل/سقوط حتمي بلا إعادة نص مفتاح + حتمية `buildMockGrade`) + API `openQuestion.test.ts` (**11**: توليد open بلا تسريب أي مفتاح، 409 تكرار النوع، خدمة الـtype، صحيح/خاطئ عبر مفتاح مقروء من DB، خلط أنواع الحمولة → 400/INVALID_ANSWER/INVALID_OPTION/INVALID_SUBMIT/INVALID_TYPE/INVALID_KIND، خطة `openQuestions`، والد 403) → **353/353 أخضر**؛ E2E `open.spec.ts` **O1** (المفتاح الزرعي «2/3» → `score 1.0` → «إجابة موفقة» + شارة إتقان، لا `answerKey`/`correctIndex` في DOM) و**O2** («توليد سؤال مقالي» لمفهوم بلا سؤال مفتوح → حقل حر → تغذية راجعة) → **40/40 أخضر**.
**لماذا**: «المدرّس الخصوصي» يحتاج تصحيح إجابة مكتوبة بجمل الطالب — لا حتمية الاختيار من متعدد تلائمها؛ والعمود الخامل `answerKey` موجود منذ PHASE 24 بلا استهلاك (بند D-028 قال بوضوح: تصحيح أسئلة المقال «مؤجل — عملة تصحيح جديدة»). البناء يجعل ضمانة **«لا تسريب المفتاح» بنيوية**: المفتاح لا يصل سوى لرسالة النظام (خيبة ذاتية حتى داخل الخادم عند تسريب الناتج)، وحارس الحرفية + سقوط قوَالبي لا يعيدانه إلى الطالب أبدًا — أوفلاين بالكامل (mock حتمي) وبدون مخطط أو اعتماديات.
**بدائل**: تصحيح عبر `correctIndex`/تطابق نص تام (مرفوض — يقتل الغرض، المقال ليس MCQ)؛ إرسال المفتاح في رسالة المستخدم مع الإجابة (مرفوض — يلوّث سياق التعلم ويشوّه إحصاءات الـusage)؛ تخزين تغذية مسبقة التوليد وإنهاء العمر (مؤجل — شحنة لكل سؤال ولو لم يُمارَس؛ عند-الطلب حتمي مع mock: نفس الشحنة تُحصى فقط عند الاستعمال)؛ تصحيح المؤلف (مرفوض — المفتاح يظهر لمستخدم بشري غير الطالب).
**إنتاج لاحقًا**: تصحيح عبر مؤشّر إعادة تصنيف دلالي (Cross-Encoder مدرب روماني — بند إنتاجي منفصل)، أسئلة مفتوحة متعددة الخطوات/متدرّجة، والتوليد الإداري بأنواع مختلطة في شحنة واحدة.

## D-035 — زمن الإجابة في معادلة الإتقان + شارات الإتقان المتدرجة (PHASE 31): السرعة مضاعِف قوة الإشارة، والشارات تتدرّج «متقن 3/5 مفاهيم»
**القرار**: إغلاق بندَي D-028/D-030 المؤجلين معًا بلا اعتماديات: (أ) **«معادلة تشمل وقت الإجابة»** — الويب يقيس الثواني بين ظهر السؤال والإرسال (`PracticeState.shownAt`) ويرسلها **في إجابات MCQ فقط** (`timeTakenSeconds` صحيح 1..600؛ الأسئلة المفتوحة لا تُقاس — زمن الكتابة ليس إشارة إتقان)؛ الخادم يتحقق ويُحيّد (غير رقمية/كسور/خارج النطاق → null) ويخزّنها في عمود جديد `answers.answer_seconds` (مخطط + migration 0008). معادلة الإتقان تضرب دلتا الصعوبة في **مضاعِف قوة الإشارة** حسب النطاق النقّي: سريع (<10ث) ×1.25، عادي (10..60) ×1، بطيء (>60) ×0.75، مجهول ×1 — السرعة تضخّم الدلتا **باتجاه واحد** أيًّا كانت النتيجة (جواب واثق سريع = إشارة قوية: صحيح يرتقي أكثر وخاطئ يُصحَّح بقوة أكبر؛ متردّد = إشارة ضعيفة في الاتجاهين)؛ **المحاولة الأولى تبقى محايدة** (صحيح 0.6 / خاطئ 0.1) مهما كانت السرعة (استمرار مبدأ D-030)، وغياب الزمن يبقي المسار القديم حرفيًا كما كان (مضاعِف الوحدة). (ب) **«تدرّج شارات إتقان أعلى»** — تعريفان جديدان في `AchievementService` (المجموع 8 → 10): `mastery_three` «متقن 3 مفاهيم» و`mastery_five` «متقن 5 مفاهيم» على حدث `mastery_achieved` بنفس عدّاد المستوى المعروض «متقن» — لا حاجة لبنوك أسئلة أوسع: يكفي إتقان مفاهيم متعددة.
1. **محرك نقي** (`progress/mastery.ts`): `AnswerTimeBand` + حدّا النطاق `ANSWER_TIME_FAST_MAX_SECONDS=10`/`ANSWER_TIME_SLOW_MIN_SECONDS=60` + `TIME_MULTIPLIER` + `answerTimeBand(seconds?)` + `timeScaledDelta(difficulty, seconds)` — كلها بلا I/O ومُختبَرة وحدات؛ `recordAssessment` يستقبل `answerSeconds?` ويطبّق `round2` على الإتقان المحدَّث للصف الموجود (قيم نظيفة للعرض؛ قيمة الاختبارات القديمة الحتمية تبقى كما هي — مضاعِف 1 وحدود صحيحة لا غبار فيها)، والمحاولة الأولى (صف جديد) تبقى 0.6/0.1 كما كانت.
2. **الخدمة والشبكة**: `SubmitAnswerInput.timeTakenSeconds?` + `normalizeAnswerSeconds` (صحيح ∈[1,600] وإلا null)؛ `gradeMcqAnswer` يخزّن `answerSeconds` في صف `answers` ويُمرّره لـ`recordAssessment`؛ `gradeOpenAnswer` يخزّن null دائمًا (الميزة MCQ فقط)؛ `routes.ts` يمرّر الحقل. العمود `answer_seconds` nullable — بلا تغيير لأي صف قائم.
3. **الويب**: `api.ts` — `submitPracticeAnswer(questionId, optionIndex, timeTakenSeconds?)` تُضمّنه عند توفّره؛ `Home.tsx` — `PracticeState.shownAt` يُثبَّت عند كل سؤال جديد (بدء/توليد/تالي)، وعلى الإرسال يُحسب `Math.round((now − shownAt)/1000)` مقصوصًا إلى 1..600 للإجابات الاختيارية فقط. شاشة الإنجازات لا تتغيّر (تُعرض التعريفات تلقائيًا).
4. **الشارات المتدرجة**: `achievementDefinitions` + `mastery_three`/`mastery_five` عبر نفس `evaluate` — لا كود جديد للحساب: عدّاد `mastery_achieved` القائم يمنح كل تعريف بلغ عتبته. المجموع الكلي 10 في كل الأسطح.
5. **الاختبار**: وحدة `masteryTime.test.ts` (**8**: حدود `answerTimeBand` الخمسة، قيم `TIME_MULTIPLIER`، `timeScaledDelta` بمضاعفات النطاق مع بقاء الحتمية التاريخية، تركيب EWMA عبر `round2` — سريع 0.79 مقابل بطيء 0.71 مقابل محايد 0.75/خاطئ 0.5/0.48) + API `practiceTime.test.ts` (**4**: التخزين + التفاوت 0.79>0.71 بين طالبَين، حيادية المحاولة الأولى في أي سرعة، إسقاط الأشكال الفاسدة/خارج النطاق (مضاعِف الوحدة)، صفر زمن على المفتوح) + API `tierBadges.test.ts` (**2**: إتقان 3 مفاهيم من خلال حلقة الإرسال الحقيقية → «متقن 3 مفاهيم» + بقاء «متقن 5 مفاهيم» مقفولة و`total`=10؛ إتقان مفهومَين إضافيَّين → «متقن 5 مفاهيم») + تحديث ميكانيكي (`achievements.test.ts` total 8 → 10) — **367/367 أخضر** + E2E `achievements.spec.ts` **E1** (صفوف الشارات 10 و`mastery_three`/`mastery_five` مقفلتان) — **40/40 أخضر**.
**لماذا**: «المدرّس الخصوصي» يقرأ ثقة الطالب من سرعته — جواب واثق سريع دليل إتقان مختلف عن جواب مكابدة بطيء، وإشراك الزمن في المعادلة يجعل الإتقان يصف الواقع أدقّ بلا أي تكلفة AI (قياس عميل + مُضاعِف نقي). وتدرّج الشارات («متقن 3 ثم 5») يكمل ربط المحرك بالإنجاز الذي وعد به D-030: الطالب يرى تقدّمًا بلا أرقام — شارة أكبر كلما اتّسع تمكّنه.
**بدائل**: معاقبة الجواب السريع الخاطئ بأكثر من مكافأة الصحيح السريع (مرفوض — يعاقب ثقة الطالب؛ الاتجاه الواحد أنظف تفسيرًا)؛ قياس زمن المفتوح (مرفوض — زمن الكتابة ضجيج لا إشارة)؛ استخدام الزمن كشرط/بوابة (مرفوض — إشارة لا حُكم؛ «لا تكذب» فسادٌ أعلى من نفعه)؛ عتبات 2/4 مفاهيم بدل 3/5 (مرفوض — الفجوة 3/5 ملموسة وتشجّع الاستمرار).
**إنتاج لاحقًا**: بقاء شارة أسبوعية («تميّز» تعاقبي) — **[نُفِّذت PHASE 32/D-036]**, وربط شارات متدرجة بمؤشرات إتقان مركّبة (مع خطأ/دقة التفصيل حسب صعوبة الدرس)، وتفرقة السرعة بحسب صعوبة السؤال (نافذة أسرع للسؤال السهل)."
- (فارغ — يُضاف عند اتخاذ قرارات جديدة، لا تُحذف القديمة)

## D-036 — التعاقب اليومي + شارات «مواظبة» (PHASE 32): أيام نشطة متتالية تُمنح «مواظب 3 أيام» و«مواظب أسبوع»، وتظهر في الخطة

**القرار**: إغلاق بند «تتابع أسبوعي» المؤجل من D-024 وبند «بقاء شارة أسبوعية (تميّز تعاقبي)» المؤجل من D-035 بلا اعتماديات ولا مخطط: (أ) **محرك نقي** (`progress/streak.ts`) يحسب **التعاقب اليومي** — عدد الأيام المتتالية المنتهية بـ«الآن» (UTC) عبر `dayKey`/`previousDay`/`streakForDates`: يوم نشط = أي صف `answers` أو `learning_sessions` (بدء جلسة) في ذلك اليوم؛ قاعدة الرأفة: اليوم الذي لم ينتهِ بعد لا يكسر التعاقب (قد يبدأ السلسلة من أمس ما دام اليوم فارغًا)؛ المدخلات المكررة/غير المرتبة تُدمج، والمستقبلية تُتجاهل. (ب) **الحساب على القراءة** في `AchievementService.streakForStudent` — استعلامان `answers.createdAt` و`learningSessions.startedAt` (بدء الجلسة = يوم النشاط) تُستخرج أُيّامهما في JS (تجاوز دالة SQLite `date()` التي تعثّرت مع مرجعَي جدولين) ثم `streakForDates` تدمج وتُعيّد. توقيت درس: عمودا الزمن مخزّنان كـ epoch ميلي-ثانية (`ts()` = `timestamp_ms`) فيصل `Date` مباشرة. (ج) **شارتان جديدتان** (المجموع 10 → 12) على حدث `daily_streak` بتقنية منح **مقارنة بالتعاقب الحالي** لا بعداد متراكم — `evaluateStreak(studentId)` يمنح كل تعريف بلغ عتبته (`streak_three` «مواظب 3 أيام»، `streak_seven` «مواظب أسبوع») عبر نفس حلقة `awardFor` المضادة للتكرار (`onConflictDoNothing`)؛ غياب يوم لا يلغي شارة مضت (الشارات دائمة). `evaluate` يفوّض `daily_streak` إليه (نفس واجهة الأحداث). (د) **نقاط حقن best-effort**: بعد أي إجابة تمرين (`masteryAfter`) وفي كل حدث جلسة (`SessionService.award`) — إعادة تقييم التعاقب أرخص من الإدراجين ولم تمسّ مسار الجواب أبدًا. (هـ) **الخطة تُفصح** عن `GET /api/practice/plan` ← `{ plan, streak }` وتُعرض الواجهة شارة «🔥 تعاقب N أيام» فوق القائمة (`data-testid="practice-streak"`، جمع عربي 1/2/3-10/11+).
**لماذا**: «المدرّس الخصوصي» يحتاج استمرارية — شارة التعاقب تكافئ الانضباط اليومي وتُبقي الطالب عائدًا، وهو البند الذي وعدت به D-024 وD-035 صراحةً؛ والحساب من بيانات التمارين والجلسات نفسها (لا عدّاد جديد ولا ساعة منفصلة) يجعل التعاقب **وصفًا حتميًا للسلوك الفعلي** يتقاسمه الطرفان.
**بدائل**: عدّاد أيام متراكم داخل جدول (مرفوض — التعاقب ليس مجموعًا؛ الحساب وقت القراءة من الأصول هو الحقيقة الوحيدة)؛ إدراج «يوم نشط» مُكرر-idempotent (مرفوض — جدول إضافي بلا قيمة فوق الأصول القائمة)؛ كسر السلسلة عند تلاشي «اليوم» قبل تمامه (مرفوض — عقاب مبكر؛ قاعدة الرأفة أنظف)؛ شارة واحدة أسبوعية فقط (مرفوض — تدرّج 3/7 يشجّع الاستمرار خطوةً خطوة).
**إنتاج لاحقًا**: تذكير يومي/أسبوعي خارجي (push/بريد) بالتعاقب، وإبراز التعاقب الأطول في تقرير الوالد، وربط التعاقب بتوصية خطة (مكافئ «حافظ على الخط»).